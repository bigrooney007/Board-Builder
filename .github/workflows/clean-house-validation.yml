name: Clean House Validation

on:
  pull_request:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Compile backend
        run: python -m compileall -q backend

      - name: Validate Recruitment clean-house contract
        run: python tests/validate_recruitment_clean_house.py

      - name: Validate Board Fundraising Game clean-house contract
        run: python tests/validate_fundraising_game_clean_house.py

      - name: Validate Board Recommitment clean-house contract
        run: python tests/validate_recommitment_clean_house.py

      - name: Validate Strategic Planning clean-house contract
        run: python tests/validate_strategic_planning_clean_house.py

      - name: Validate homepage Board Applicant Network contract
        run: python tests/validate_homepage_applicant_network.py

      - name: Validate fully preloaded Admin preview fixtures
        run: python tests/validate_admin_preview_fixtures.py

      - name: Validate Admin dashboard preview fixtures
        run: python tests/validate_admin_dashboard_previews.py

      - name: Validate Admin client testing center
        run: python tests/validate_admin_client_testing.py

      - name: Validate platform communications
        run: python tests/validate_platform_communications.py

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: yarn
          cache-dependency-path: frontend/yarn.lock

      - name: Install frontend dependencies
        run: yarn --cwd frontend install --frozen-lockfile

      - name: Build frontend
        env:
          CI: "false"
          REACT_APP_BACKEND_URL: "http://localhost:8000"
        run: yarn --cwd frontend build
