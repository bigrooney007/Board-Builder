# Nonprofit Board Builder PRD

## Original Problem Statement
Build only one professional green Nonprofit Board Builder landing page, one four-step board assessment form, one confirmation screen, MongoDB storage for every submission, and one owner email containing every answer. No accounts, dashboards, payments, AI, recruitment automation, CRM, scheduling, scraping, background jobs, or additional pages. Use the user-provided logo and the exact supplied content, questions, options, and confirmation messaging.

## Architecture Decisions
- React single-page experience with three states: landing, assessment, confirmation.
- FastAPI `POST /api/assessments` validates and stores submissions in MongoDB.
- Each submission receives a `NBB-YYYYMMDD-XXXXXX` number, UTC timestamp, and `New Board Assessment` status.
- Resend owner notification is attempted after persistence; email failure never rolls back the saved assessment.
- Email settings are environment-driven through `RESEND_API_KEY`, `SENDER_EMAIL`, and `OWNER_NOTIFICATION_EMAIL` (or `OWNER_EMAIL`).

## Implemented
- Responsive landing page with navigation, hero, three transformation stages, six discovery cards, calls to action, supplied logo, and footer.
- Added seven exact client testimonials in a three-featured/four-supporting layout with initials avatars and local approved photo/logo replacement controls.
- Added four static, clearly labelled Sample tool demonstrations using fictional Bright Futures Literacy Initiative information only.
- Added Rooney Akpesiri's complete founder story, supplied photograph, local photograph replacement control, five-item credibility strip, and assessment CTA.
- Updated homepage navigation anchors and final CTA while preserving the existing assessment flow and backend.
- Four-step assessment containing every required and optional field, exact options, validation, progress, Back/Next controls, phone helper text, and confirmation checkbox.
- Complete database submission, unique metadata, owner email template with all answers under four required headings, and confirmation screen.
- One E2E assessment was submitted and fully verified in MongoDB; confirmation and mobile layout passed.
- Final homepage verification passed after correcting the three tool caption titles; no product defects remain in the added homepage sections.
- Added required nonprofit `execution_preference`, separate weekly-email consent metadata, owner-email inclusion, duplicate-safe nonprofit contact records, and exact Resend nonprofit syncing.
- Updated applicant consent wording and duplicate-safe applicant syncing to the exact Board Applicants Segment and applicant Topics.
- Created four exact Resend Segments and three Topics for nonprofit leaders, board applicants, and both 72-hour No Action audiences.
- Added duplicate-safe weekly nonprofit and applicant aggregate Broadcast automation, with editable Monday/Wednesday Eastern schedules intentionally disabled pending owner approval.
- Added secure tracked text actions for board transformation readiness and applicant availability, mobile SMS handoff, desktop copy controls, click records, and No Action segment removal.
- Added 72-hour No Action processing, backend report/recipient records, aggregate-only email content, native unsubscribe/preference links, and deduplicated owner automation-error emails.
- Updated Privacy Policy for weekly emails, aggregate reporting, consent records, tracked engagement, correction/deletion, Resend processing, and no sale of personal information.
- Single isolated E2E verification passed 11/11 backend checks plus public UI flows; test Broadcasts targeted an owner-only temporary segment and never the live audiences.

## Prioritized Backlog
- **P0:** Provide `RESEND_API_KEY`, `SENDER_EMAIL`, and `OWNER_NOTIFICATION_EMAIL`/`OWNER_EMAIL` in backend environment; owner delivery cannot run without them.
- **P0:** Repeat the single E2E submission after email configuration and confirm one delivered email contains all answers.
- **P1:** None within approved scope.
- **P2:** None within approved scope.

## Weekly Automation Activation
- `NONPROFIT_WEEKLY_REPORT_ENABLED=false`
- `APPLICANT_WEEKLY_REPORT_ENABLED=false`
- Both remain disabled by explicit owner choice until approval to activate.

## Next Tasks
1. Add the missing owner-email environment settings.
2. Repeat the specified E2E flow once to verify delivery.

## Funnel Architecture + Offer Sales Pages (June 2026) — Built, TESTING PENDING (user held testing)
- Final funnel confirmed already in place and preserved: Home (Tell Us About Your Board) → /board-transformation (assessment) → /board-transformation/result/:token (Main Board Transformation video + 3 pathway buttons) → /offer/{recruitment|reactivation|activation} → Stripe → existing post-payment intake → DIY platform / DWY Calendly-intake path.
- The 3 /offer/* pages converted from video pages to text-based sales pages (same routes, same OfferVideoPage.jsx component file). Individual offer videos + placeholders REMOVED. Only video in the funnel is the main Board Transformation video.
- Exact owner-supplied sales copy for Recruitment, Reactivation, Activation stored verbatim in SITE_CONTENT.offerSalesPages (siteContent.js — replaced old offerVideos block; single canonical source, no hardcoded copy in component).
- Payment untouched: DIY/DWY buttons call the same 6 existing checkout endpoints (diy-checkout, direct-project-checkout, reactivation-diy/project-checkout, activation-diy/project-checkout). No Stripe products/prices changed. Guarantee line "Your investment is protected by my 100% refund guarantee." rendered under every payment button on these pages.
- Testimonials: existing TestimonialCarousel retained on all three pages. Closing CTA sections with repeat buttons for Reactivation/Activation (per copy); Recruitment closing has no repeat buttons (per copy).
- New CSS: .offer-sales-* block appended to App.css (responsive, cream body box, DIY white card / DWY forest card).
- NOT TESTED YET: user instructed "do not run any test until i say so". No deployment. Next: await user approval, then verify funnel routing + checkout initiation + mobile/desktop rendering.
## Phase 4 (Blog + Lead Nurture) — Complete
- Public /blog + /blog/:slug pages with 3 categories, deterministic category CTAs, homepage "Latest From Nonprofit Board Builder" compact slider (max 6, prev/next/swipe).
- Claude blog generation (marketing_service.py) with strict voice/writing rules, validation (word count, em dash, banned phrases, stats claims, duplicate slug/title), one automatic correction attempt, owner failure emails, unique category+scheduled_date key. Schedule: Mon Recruitment / Wed Reactivation / Fri Activation 8:00 AM ET.
- Lead nurture: 3 Resend segments (Recruitment/Reactivation/Fundraising Activation Leads), one active category per contact (latest offer wins), 12 fixed approved templates (no AI), Tuesday 7:00 AM ET rotation 1→4 with duplicate-week protection, failed sends never advance rotation, owner error alerts, recruitment purchase removes lead from nurture (nurture_status=customer). Board Applicants excluded from all recurring emails.
- Flags remain false: BLOG_AUTOMATION_ENABLED, LEAD_NURTURE_ENABLED (plus BOARD_APPLICANT_OPPORTUNITY_EMAILS_LIVE, RECRUITMENT_97_LIVE, RECRUITMENT_497_LIVE). Owner enables after approval.
- Final Phase 4 test: backend all pass; frontend 22/22 pass. 3 test blog posts remain published for review.

## Blog Preview Dashboard (June 2026) — Complete
- Automation now creates drafts as "Pending Review" (marketing_loop publish_now=False); owner alert email sent via Resend when a draft is ready. Drafts stay pending until acted upon (no auto-publish timeout, per user choice).
- New admin endpoints: GET /api/admin/blog/posts, PATCH /api/admin/blog/posts/{id} (inline edit, slug updates only for unpublished), POST .../approve, .../reject, .../regenerate. POST /api/blog/generate now defaults publish_now=false.
- New "Blog Posts" tab at /admin (AdminPage.jsx: BlogAdminSection + BlogPreview): status badges, per-category "New Draft" buttons, full rendered preview with CTA, inline edit form, Approve & Publish / Reject / Regenerate. Published posts: edit only (409 guards on approve/reject/regenerate).
- Tested: iteration_6.json — backend 11/11 pass, frontend Playwright all pass. Regression suite: /app/backend/tests/test_blog_preview_dashboard.py.

## Conversion + Blog Copy Update (June 2026) — Complete
- /recruit/options rewritten: headline "Choose the Level of Support You Want Recruiting Your Board"; concise transformation cards — $97 Do It Yourself, $497 Self-Guided Recruitment (Most Popular ribbon), $3,497 Done With You (Calendly link); no feature lists on recruitment cards; testimonials moved BELOW offers on /recruit/options, /reactivate/options, /activate/options (content unchanged).
- Recruitment form: two overlapping questions merged into multi-select accomplish_areas (19 options + conditional Other textarea); new required support_preference radio-cards (diy/self_guided/done_with_you/undecided); still 3-step with progress/back; redirect direct to /recruit/options; matching offer gets "Matches the support you selected" badge via sessionStorage.
- Backend: REQUIRED_ANSWERS updated, support_preference validated + stored top-level on lead, owner email includes "Preferred Level of Support" and "What They Need Their New Board Members to Help Accomplish". Reactivation/Activation funnels unchanged.
- Blog: prompt + validation retargeted to 350-550 words (abs max 650, min 300), straight-to-the-problem one-idea style, no step-by-step guides, short-paragraph check. Schedules/duplicate protection/CTAs untouched.
- Tested: iteration_7.json — frontend 100%, backend 11/12 (blog live generation blocked: EMERGENT_LLM_KEY budget EXHAUSTED — user must add balance; validation rules unit-tested and passing).
- KNOWN: /app/backend/tests/test_blog_preview_dashboard.py still assumes old 700-1100 word rule (do not use as authority).

## Recruitment Funnel Upgrade + Owner Review Mode (June 2026) — Complete
- One-offer journey: /recruit (hero no CTA -> 3-step form -> testimonials) -> /recruit/process (LOCKED 6-stage framework page, both CTAs -> /recruit/checkout) -> /recruit/checkout ($497 Guided Board Recruitment only; What You Walk Away With incl. clickable Recruitment Guarantee modal; Before You Start; terms checkbox recorded in terms_agreements collection; existing Stripe tier 497 reused). /recruit/options no longer in the active journey (route kept).
- Support question reworded: "What Kind of Support Would Be Most Helpful to You?" values diy/guided/done_with_you/undecided (self_guided still accepted server-side for compat).
- Owner Review Mode (TEMPORARY, env OWNER_RECRUITMENT_REVIEW_MODE=true + admin auth only): banner, blank-form navigation (no lead/email), "Continue in Owner Review Mode" checkout bypass -> modules 1-6 via review_mode_member fallback in authenticate_member; progress writes skipped; no side effects. /admin has "Review Recruitment Experience" button. Set flag false after owner's second test.
- RECRUITMENT_GUARANTEE_TERMS env added (EMPTY — owner must supply final terms; fallback generic copy shown, configured=false). No dedicated Refund Policy document exists (checkbox links to /terms).
- After payment: account creation now lands on Module 1 (self-guided/basic) instead of dashboard.
- SEO: usePageMeta/PAGE_META (seo.js) + index.html defaults for title/description/OG/Twitter across home, recruit, process, checkout, reactivate, activate, blog.
- Tested: iteration_10.json — backend 14/14, frontend all product checks pass. Regression suite /app/backend/tests/test_recruitment_funnel_upgrade.py.

## Landing Page Hero Consolidation (June 2026) — Complete
- /recruit, /reactivate, /activate: duplicate hero removed; colored banner (dark green gradient + grid + circle) is now the hero containing label/title/subtitle (existing copy verbatim); no hero CTA; order Banner -> Form -> Testimonials on all three. CSS: .funnel-hero-banner. Old .funnel-hero/.funnel-hero-mark styles unused on these pages. Verified via screenshots desktop + 390px mobile.

## Recruitment Modules 1–6 Execution Flow Correction (June 2026) — Complete, iteration_13 100% pass
- Module 1: five-step wizard REMOVED; one-page skills/gaps intake (current skills, desired skills, optional notes — 23 standard options + Other); org info never re-asked; profile auto-saves + auto-confirms as part of "Identify the Board Members I Need" (MaterialCard beforeGenerate).
- Module 2: intake + Generate in one panel; required marks; answers save automatically as part of Generate (no hidden Save).
- Module 3: custom-question builder/+ icons REMOVED; "Generate My Board Application" = standard core-question form via NEW non-AI endpoint POST /api/workspace/opportunity/application/generate (auto-saved, link + Copy Link immediately visible); Personal Invitation Email shown only when Module 2 intake says founder knows people; Publish Recruitment Campaign remains the only email/publish trigger.
- Module 4: external applicant intake = Name + CV only (backend email now optional, source "LinkedIn / External"); per-applicant View Application / View CV / Generate Interview Guide / Interview Invitation; decision emails REMOVED from Module 4.
- Module 5: three subsections — Verify (reference tools for ALL applicant sources + background-check search), Decide (Conditional Board Appointment Email + After-Interview Rejection, both now module 5), Prepare (Organization Overview, Board Manual, 3 agreements — now org-level per_application=False module 5 — + Board Member Profile Form panel moved here).
- Module 6: shows Selected members; signature workflow uses org-level agreements (prepare falls back to application_id="" material); Module 5 docs shown read-only ("prepare it in Module 5" if missing); Onboarding Script kept in Module 6 (org-level); NEW first_board_meeting_invitation generation type + panel asking only meeting details.
- Regression suite: /app/backend/tests/test_modules_1_to_6_refactor.py (no Claude calls). Test data cleaned from owner-review-admin tenant after run.
- Known minor (deliberately not built per owner's no-new-features rule): no DELETE endpoint for applicants.

## Platform Refinement + Reference Library (June 2026) — Complete
- Owner Review auth fix: authenticate_member falls back to review-mode admin; checkout enterReview refreshes member context; no second login. Verified iteration_11 (all pass).
- Testimonials LOCKED in /app/frontend/src/components/testimonialsData.js (8 exact founder-supplied entries incl. 2 Donna Kargel + Cyrena verbatim; typos preserved; no Result rows). All carousels/sections import it. Duplicate React keys fixed in both testimonial components.
- /recruit/process + /recruit/checkout centered premium visuals. Modules 1-6 execution tools: Module1 Powerhouse Board Blueprint (verified live gen, iteration_12), Module2 strategy intake (PUT /workspace/strategy-intake), Module3 9 tools + application link copy, Module4 general invite/rejection + external applicant CV upload + conditional offer/after-interview rejection, Module5 background-check browser search, Module6 onboarding script + share links (POST /workspace/materials/{id}/share, public /shared/{token}) + Board Member Profile Form (/board-profile/{token}). Agreements keep signature links only (422 on share).
- Recruitment Execution Reference Library (founder-only, /admin Reference Library tab): admin CRUD /api/admin/reference-library, docx/pdf/txt extraction (tables incl., 120k cap), module-aware retrieval (slice_reference, max 2 docs) appended to generation context with privacy rules + source priority; ONLY approved materials used. Founder doc 'For All The Children' (reference_id 860f14a8d196abf2, 65,772 chars) uploaded, status NOT APPROVED — owner must approve in /admin before it is used. Tested iteration_12: 17/17 backend + UI pass; regression suite /app/backend/tests/test_reference_library.py (issues 1 Claude call).
- Upgraded retrieval (June 2026): multi-window keyword retrieval in workspace_service.py — GENERATION_KEYWORDS per generation type (29 types) prioritized over MODULE_KEYWORDS fallback (fallback capped 4,000 chars); windows aligned to paragraph boundaries (max 3,000 chars each), overlapping windows merged, total budget ~8,000 chars shared across max 2 docs; empty string returned (no reference block) when no keyword match or no approved docs. Dry-tested against full 65,772-char doc: all 29 generation types within budget, unapproved exclusion + PII rules verified, no live Claude calls.
- Reference Library UI fix (June 2026): table replaced with responsive card list in AdminPage.jsx (ReferenceLibrarySection) — prominent "Approve for AI Reference" / "Revoke Approval" button, status badge "Not Approved" / "Approved for AI Reference", Delete link; mobile stacks full-width (App.css .reference-card). Doc remains NOT APPROVED — owner approves manually. Admin-only 401 protection verified.
- NOTE: ElevenLabs request from earlier was superseded by user's pivot; not built.

## Batches 10 + 11 — Offer Positioning, Advisory Board, Owner Review Persistence, Final E2E (June 2026) — Complete
- Checkout ($497 unchanged, single offer): hero badge "Start Recruiting Skilled Professionals in Less Than 30 Minutes" (checkout-thirty-minute), "Start Recruiting in Less Than 30 Minutes" benefit section, "Build the Board Your Organization Needs" governing/working/advisory section. Copy scoped to STARTING recruitment.
- Advisory Board: /recruit form board_type now includes "An advisory board that provides expertise, strategic advice and connections"; workspace_service.profile_context_text emits SELECTED BOARD TYPE line; ai_service SYSTEM_MESSAGE expanded advisory rules (expertise/advice/connections focus; never auto-assign fiduciary/governing/director-vote/ED-oversight duties). Live Claude advisory generation verified (terminology + no fiduciary + human writing). No separate product/entitlement/funnel.
- Owner Review persistence: admin_review_progress collection; POST /api/review-mode/progress (admin + flag required, stores last_route/last_label/updated_at), GET, DELETE (reset). Frontend ReviewProgressTracker (reviewMode.jsx, mounted in App.js) auto-posts on recruitment-route navigation in review mode. /admin ReviewResumePanel: Last reviewed + Last activity + Resume Recruitment Review + Start From Beginning (resets position only). Logout terminates auth normally; no fake customer records.
- Batch 7-9 gap closure: Module 2 referral-network question (Yes/No/Not Sure, intake-referral-network); recruitment_strategy customer view = "Your Recruitment Strategy Is Ready" + channels + Download (strategy-ready, full text not dumped); Module 1 "Download Detailed Board Recruitment Profile" (download-detailed-blueprint); Module 3 reorder (ApplicationPanel first w/ auto-created app + link, then Board Opportunity as first generator, approvable Generate/Edit/Save/Approve on ALL Module 3 cards, public tools hidden when recruit_outside=No, personal cards gated on know_people, referral cards gated on referral_network); publish wording de-jargonized (no "TEST MODE" customer-facing); Admin CSV import UI (admin-import-toggle: file + consent + Preview [commit=false dry-run, no writes] + Confirm Import) on /admin applicants tab.
- Pastor Cyrena testimonial updated in canonical testimonialsData.js ("Because of your input..." — "uh," removed). Single source, propagates platform-wide.
- Final verification: iteration_14.json — 15/15 backend pytest + all UI checks pass (test_batch11_final_verification.py regression file, no AI/email side effects). ONE blocking defect found+fixed: ReviewProgress model missing `label` (500 on progress POST) — re-verified full cycle. Self-tests: Module 2/3 delta UI (review mode), advisory context line, one live advisory Claude generation. Deep Module 4-6 flows covered by iteration_13 baseline (unchanged since).
- ENV: BOARD_APPLICANT_OPPORTUNITY_EMAILS_LIVE restored to "true" after test-safe verification. OWNER_RECRUITMENT_REVIEW_MODE still "true" — owner should set false after finishing review. RECRUITMENT_GUARANTEE_TERMS still EMPTY (owner must supply). RECRUITMENT_497_LIVE still false (owner enables to sell publicly).
- All 11 batches of the Recruitment refinement instruction are COMPLETE.

## Post-Verification Correction Batches 1-3 (June 2026) — Complete, iteration_15 100% pass
- Batch 1: Module 1 detailed-profile download REMOVED (internal detailed_roles kept for downstream). Module 2: new concise recruitment_strategy schema (2-sentence exec summary, roles one-liners, brief applicable channels, selection criteria) + FIXED platform-authored 3-week timeline (Day 1 launch / 48h interviews / Weeks 1-2 / Week 3 onboarding) + Step 2-6 locked roadmap + Your Next Step (constants in ai_service.py). Board Recruitment Details intake (meeting_frequency+other, board_term+other, max_board_size+unknown, meeting_format+location, time_expectation, application_deadline+date) saved in strategy_intake, flows downstream. Server-side reportlab PDF GET /api/workspace/strategy-pdf (branding/logo, margins, KeepTogether page breaks, footer org+page number, no route URLs); strategy-ready panel has PDF download + "Continue to Step 3" CTA. Public Board Application page simplified: logo + org name + server-built intro_sentences (org/mission/board-type, 3 sentences) + form only — Board Opportunity dump/facts removed. Interview Invitation short-message generation types (general + per-applicant) with Copy. Recruitment Results blank-page BUG root cause: route/component never existed — built ResultsPage.jsx (/app/recruitment/self-guided/results): summary counts, Joined/In Progress/Not Selected groups, per-member Board Member Portfolio + portfolio email cards, empty state, error state + retry, Book a Call With Rooney (calendly.com/boardbuilder/recruitboard).
- Batch 2: Module 5 "Decide Who Moves Forward": existing applicants as candidate cards (interviewed-first, name/title/employer/status/interview), Review + Move Forward / Do Not Move Forward via POST /api/workspace/applications/{id}/decision (statuses "Moving Forward"/"Not Moving Forward" added). Move Forward idempotently prepares (NO emails): reference_process, signature_requests for Approved agreements ("Ready for Signature"), board_profile_link w/ prefill. Candidate progress: CandidateStatusList (reference/background/3 agreements independent/profile/conditional) + status-aware next-step hint. Hosted agreement signing page /sign/{token} redesigned: branded (logo/org/primary color), noindex, readable document, typed AND drawn (canvas) signatures, method/image/date/version recorded, immutable after signing (template edits create new versions only), Agreement Signed state w/ signed record + Download Copy; nonprofit archive PDF endpoint unchanged.
- Verification: iteration_15.json — backend 11/11 pass (tests/test_batch3_verification.py, safe regression file), all UI checks pass, zero blocking defects. Live-Claude strategy/short-message content generation relies on updated schemas (validated structurally, not all live-generated).

## Production Quality Correction (June 2026) — Complete, iteration_16 100% pass
- Finished-content standard: SYSTEM_MESSAGE FINISHED CONTENT STANDARD block (no placeholders/template tokens, scheduling-link exception, no invented facts, no founder-advice text, flush-left paragraphs) + _flush_left sanitizer on ALL structured_to_display output. Live Claude check passed (real org/mission/URL, zero placeholders, no leading indentation).
- Org name + mission REQUIRED: Module 1 asks once (module1-organization-name prefilled from lead, module1-mission textarea); generate endpoint gates with 422 until both exist; mission carried via profile_context_text MISSION STATEMENT line into all generation.
- Branding reused everywhere: existing BrandingPanel (logo/colors) now feeds server-side reportlab PDFs for EVERY material (new GET /api/workspace/material-pdf/{material_id} — logo, brand color headings, page-number footer, no URLs; MaterialCard Download PDF + Results portfolio PDF now use it), hosted sign pages, and branded hosted shared pages (GET /api/shared/{token} returns org+branding; SharedResourcePage redesigned with hosted-agreement styling + noindex).
- Hosted-link-first workflow: overview/manual keep View Online/Copy Secure Link/Download PDF; agreements primary action is Review & Sign online (CandidateStatusList Copy Review & Sign Link buttons; signatures API returns token); sign page signature options: Type My Name to Sign / Use My Signature (draw on canvas OR upload PNG/JPG ≤200KB, method 'uploaded' stored); signed archive/immutability preserved. Conditional Appointment email auto-inserts shared/sign/profile links, excludes Signed agreements.
- Module 5 UI: "Communicate Your Decision" removed → "Schedule the Board Onboarding Session" with natural questions (date/time/timezone select/format + conditional link/location + optional preparation), saved once and reused across candidates.
- Verification: iteration_16.json — backend 15/15 pass, all UI checks pass, zero defects. NOTE: production (nonprofitboardbuilder.com) redeployed earlier this session BEFORE this quality correction — needs another Redeploy to ship these changes.
- Branding timing fix (deployment-readiness): BrandingPanel (logo + primary/secondary color, optional, ask-once) now also shown in Module 1 after org name/mission (module1-branding-section); Module 5 panel retained as add/update fallback; same /workspace/branding org record reused everywhere; neutral fallback when skipped. Verified via screenshot (both modules) + iteration_16 branding-reuse backend tests.

## Deployment + Owner Review Reset (June 2026)
- Production (nonprofitboardbuilder.com) redeployed twice: (1) full quality-correction build (all routes + new bundle markers verified live), (2) small redeploy shipping admin-only POST /api/admin/reset-review-data (no UI; authenticate_admin; deletes ONLY user_id/owner_user_id == "owner-review-admin" docs across all collections + admin_review_progress; idempotent; returns per-collection counts).
- Preview owner-review reset done directly in Mongo; production reset executed via the endpoint: 17 docs deleted (recruitment_profiles 1, generated_materials 11, opportunities 1, opportunity_applications 1, board_profile_forms 1, share_links 1, admin_review_progress 1), 0 remaining, verified empty review workspace + review mode still active. Founder Reference Library, Board Applicant Network, customers untouched (0 matches, read-only verified by deployer agent).
- Owner will now personally re-run the Recruitment Review from scratch on production.
- Temporary reset endpoint REMOVED afterward and production redeployed (endpoint 404 with/without admin auth on prod; /admin, review mode, Module 1 verified live; iteration_17 preview verification 100% pass).

## Recruit With Rooney — 3-Batch Direct $997 Offer (June 2026) — Complete, iteration_18 100% pass
- Batch 1 (sales page): /recruit-with-rooney (RecruitWithRooneyPage.jsx, direct link only, NOT in nav). Exact headline "Recruit the Board Members Your Nonprofit Needs in 2 Weeks" + "Build your board with me."; dominant 16:9 video area driven by backend env DIRECT_RECRUITMENT_VIDEO_URL (GET /api/direct-recruitment/config; empty -> branded "Board Recruitment Video Coming Soon" placeholder, no rebuild needed to set URL); "100% Refund Guarantee" with exact copy "Your investment is protected by my 100% refund guarantee." (View Guarantee Terms link/modal only renders if RECRUITMENT_GUARANTEE_TERMS configured — currently empty so hidden); struck $2,497 Standard Price + "Take Action Today: $997" + "GET STARTED NOW — $997"; testimonials "See What Other Nonprofit Leaders Have Said" from canonical testimonialsData.js via TestimonialCarousel new optional priorityNames prop (Cyrena, Tony Barnes, Martina Jones, Devona Boone, Donna Kargel first; default order everywhere else unchanged). FunnelLayout gained `restrained` prop (logo-only nav, same footer). SEO meta recruitWithRooney added.
- Batch 2 ($997 payment -> Module 1): Stripe product/price recruit_with_rooney_997 ($997 one-time, test price price_1U3Ow5DyMuVBxFnGdtuZFb5G in STRIPE_RECRUIT_WITH_ROONEY_997_PRICE_ID; setup_stripe.py CATALOG entry added). POST /api/payments/rooney-checkout (no lead required; gated by RECRUIT_WITH_ROONEY_997_LIVE=false -> public 403 "Enrollment is not open yet" shown as "Enrollment opening shortly."; admin internal_test allowed; cancel -> /recruit-with-rooney?checkout=cancelled with friendly notice). claim_recruitment_purchase accepts offer_source=recruit_with_rooney tier 997 -> entitlement recruitment_self_guided (SAME recruitment system, no duplicate program) and stamps purchase_source=recruit_with_rooney_997, offer="Recruit With Rooney", support_program, price_paid=997 (existing $497 purchases distinct — no purchase_source field). claim/register/login responses return claimed_source; PurchaseSuccessPage routes rooney buyers (new or already-authenticated) directly to /app/recruitment/self-guided/module/1, bypassing dashboard. Login now honors ?next= (AuthPages) and CoursePages redirects logged-out users to /login?next=<module>.
- Batch 3 (21-day accountability): /app/backend/accountability_service.py + rooney_engagements collection (unique purchase_id index; loop started in server.py, 15-min cadence, 8:00-20:00 America/New_York send window). Enrollment only from verified $997 claims (one active engagement per member; duplicate purchases don't overlap). Immediate welcome "Welcome — Let's Build Your Board" (exact supplied copy, START BUILDING MY BOARD -> Module 1). Check-ins days 2..20 + final day 21, each idempotent (atomic per-day claim; retries never duplicate; missed days recorded skipped, only latest due day sent). Progress read live from course_progress before EVERY send: stage not_started/module1..6 templates (exact supplied wording) + deterministic rotation openings/subjects for repeated same-stage sends. Early completion (all 6 modules) -> single "You Built Your Board" completion email linking to Results, sequence stops. Day 21 final check-in stage-aware, then status ended; no day 22+. Refunded purchase (purchases.payment_status=refunded) -> ended_refunded, no sends. Sender "Rooney Akpesiri <nonprofits@nonprofitboardbuilder.com>", reply-to rooney@nonprofitboardbuilder.com; no Claude calls; no placeholders; failures alert owner via existing send_automation_error. Send history stored on engagement.sends (email_type "Recruit With Rooney Accountability", day, module, stage, variant, subject, sent_at, email_id).
- Verification: iteration_18.json — backend 13/13 pytest (regression suite /app/backend/tests/test_recruit_with_rooney_e2e.py, Resend monkeypatched, time simulated via backdated purchased_at + process_accountability(ignore_window=True)); all Batch-1 UI checks incl. 390px mobile; $497 flow, homepage, testimonial order regression pass. Zero defects.
- OWNER TO-DOs before selling: supply YouTube URL in DIRECT_RECRUITMENT_VIDEO_URL; set RECRUIT_WITH_ROONEY_997_LIVE=true to open public enrollment; deploy to production (also run setup_stripe.py against LIVE Stripe key on prod to create the live $997 price and set the prod env var). NOTE: preview public URL intermittently serves a stale static homepage snapshot ("Frontend Preview Only" toast) for all routes — app itself is correct (verified on the live dev server); backend APIs on the public URL work fine.

## Board Recruitment Project Proposal — Direct $1,998.50 Engagement Page (June 2026) — Complete, iteration_19 100% pass + full paid E2E self-test
- Public direct-link page /board-recruitment-proposal (BoardRecruitmentProposalPage.jsx, restrained header, standard footer, NOT in nav) for nonprofits that contacted Rooney directly. Section order: hero ("Build the Board Your Nonprofit Needs" + Board Recruitment Project Proposal eyebrow, exact supplied copy incl. bold objective) -> "Here Is How We Will Work Together" (3 numbered step cards with exact copy/bullets/onboarding docs) -> "The Result" ("In Two Weeks, You Will Have the Board Your Nonprofit Needs.") -> "Project Timeline" (prominent "2 Weeks") -> "Your Investment" (struck $3,997, 50% discount line, "TODAY: $1,998.50") -> "100% Money-Back Guarantee" (exact copy; terms link only if RECRUITMENT_GUARANTEE_TERMS configured — currently hidden) -> "Ready to Build Your Board?" + CTA "MAKE PAYMENT AND LET’S GET YOU STARTED" -> testimonial slideshow "See What Other Nonprofit Founders and Leaders Have Said" (ALL 8 canonical testimonials via TestimonialCarousel, default order, word-for-word incl. current Cyrena).
- Payment: POST /api/payments/direct-project-checkout (public, no live-flag gating — proposal is sent personally by Rooney) -> Stripe one-time $1,998.50 (catalog direct_board_recruitment_project 199850; test price price_1U3iBEDyMuVBxFnGzSzg194L in STRIPE_DIRECT_BOARD_RECRUITMENT_PRICE_ID). Metadata/payment_transactions record purchase_source=direct_board_recruitment_project, offer="Board Recruitment Project". Success -> /board-recruitment-proposal/confirmed?session_id= (polls existing verified /api/payments/status; paid -> "Payment confirmed" then auto-redirect to https://calendly.com/boardbuilder/recruitboard; missing/fake session shows safe failure card, never redirects). Cancel -> /board-recruitment-proposal?checkout=cancelled with friendly notice. No member account, no entitlement, no Module 1 (claim-purchase rejects this offer_source with 400). $497 and $997 flows untouched.
- Verified: iteration_19.json 100% (8/8 backend pytest incl. idempotency + regression; full UI incl. mobile 390px; regression suite /app/backend/tests/test_board_recruitment_proposal.py). Main agent additionally completed a REAL test-mode card payment end-to-end: Stripe charged exactly $1,998.50, confirmed page verified payment, browser landed on Rooney's Calendly (Meet With Rooney, 30 min). Test transactions cleaned from preview DB.
- Production URL after deploy: https://nonprofitboardbuilder.com/board-recruitment-proposal (prod needs setup_stripe.py run with LIVE key + STRIPE_DIRECT_BOARD_RECRUITMENT_PRICE_ID env var, same as other offers).

## Board Recruitment Funnel Simplification — 3 Batches (June 2026) — Complete, iteration_20 100% pass
- BATCH 1 — /about-rooney (AboutRooneyPage.jsx, restrained header + standard footer, NOT in nav): Linktree-style personal lead page. Hero = existing founder photo asset + exact "Rooney Akpesiri" + "The Nonprofit Board Builder" + canonical mission statement reused word-for-word from FounderStorySection ("Today, I teach, coach, and work directly with founders and executive directors so they do not make the mistakes I made, damage their relationships, or waste their time."). Then TestimonialCarousel (all 8 canonical, heading "What Nonprofit Founders and Leaders Have Said") -> "Two Ways I Can Help You Recruit Your Board": DIY $497 card ("I WANT TO RECRUIT MY BOARD MYSELF" -> /recruit-your-board-yourself) first, Do It With Me $1,998.50 card ("I WANT TO RECRUIT MY BOARD WITH ROONEY" -> /board-recruitment-proposal). No payment/forms on this page.
- BATCH 2 — Offer pages: NEW /recruit-your-board-yourself (RecruitYourBoardYourselfPage.jsx): hero "Recruit Your Board Yourself", offer copy, "When You Finish" icon list, "Your Investment" $497 / One payment. / No monthly membership., CTA "MAKE PAYMENT AND START RECRUITING MY BOARD" -> POST /api/payments/diy-checkout, secondary "Want Me To Do It With You?" -> proposal. UPDATED /board-recruitment-proposal: hero reduced to eyebrow + H1 + one supporting line; "Project Summary" moved below hero into white area (brp-summary-section); CTA text now "MAKE PAYMENT — LET'S START RECRUITING YOUR BOARD TOGETHER"; secondary "Prefer to Do It By Yourself?" -> DIY page; listicles converted to refined green Check icon lists (.icon-list, no big indentation).
- BATCH 3 — Payment -> shared intake -> Calendly: NEW POST /api/payments/diy-checkout ($497 one-time, test price price_1U3j7WDyMuVBxFnGSarAVDqG in STRIPE_DIY_BOARD_RECRUITMENT_497_PRICE_ID, product prod_V3qpNWSIS9WZiI with tax_code txcd_10000000 — REQUIRED for managed payments; metadata purchase_source=direct_diy_board_recruitment_497, offer_source=direct_diy_board_recruitment, tier 497). DIY success -> /purchase/success (existing account create/login; claim grants recruitment_self_guided, product "Do It Yourself Board Recruitment", NO accountability emails) -> then routed to /board-recruitment-intake?session_id= (NOT Module 1). DWM direct-project-checkout success_url changed to /board-recruitment-intake?session_id= (confirmed page also re-targeted to intake; no direct Calendly). NEW shared intake /board-recruitment-intake (BoardRecruitmentIntakePage.jsx) + backend board_intake_routes.py (GET /api/board-recruitment-intake/context 404 bogus / 402 pending, POST /submit): gated by verified paid session with qualifying purchase_source {direct_diy_board_recruitment_497, direct_board_recruitment_project}; 5 steps (About You/Org with prefill, LinkedIn org+personal Yes/No with conditional URL fields, Present Board with Not Sure checkboxes, Board You Need 16-skill checklist, Logistics reusing canonical field names); upsert by session_id into board_recruitment_intakes (idempotent); for members merges non-destructively into recruitment_profiles.data + strategy_intake (existing values win). After submit BOTH offers redirect to https://calendly.com/boardbuilder/recruitboard. Cancel URLs: DIY -> /recruit-your-board-yourself?checkout=cancelled, DWM -> /board-recruitment-proposal?checkout=cancelled.
- No email automation touched. $497 homepage funnel, $997 flow + accountability, Modules 1–6, Applicant Network, Reactivate/Activate untouched (regression verified).
- Verified: iteration_20.json 100% (17/17 backend pytest incl. real DIY $497 test-mode payment E2E, intake gating/idempotency, mobile 390px; test file /app/backend/tests/test_batch_intake_e2e.py).
- Production deploy needs: STRIPE_DIY_BOARD_RECRUITMENT_497_PRICE_ID created with LIVE key (product must have tax_code) + code deploy.

## Launch Prep for iteration_20 + Intake Owner Notification (June 2026) — Complete, deployment_agent PASS
- Intake notification: board_intake_routes.py submit now emails OWNER_NOTIFICATION_EMAIL (via Resend, NONPROFIT_SENDER) a full field table when a paying customer FIRST submits the intake (only on upsert insert — resubmits don't re-send; failures logged, never block submit/Calendly redirect). Tested live (sent to delivered@resend.dev, env restored).
- DIRECT_RECRUITMENT_VIDEO_URL set to https://youtu.be/j1_NAkwq3mo (video verified rendering on /recruit-with-rooney).
- Live $497 DIY price: setup_stripe.py CATALOG got ("diy_board_recruitment_497", "Recruit Your Board Yourself", 49700). payment_routes.resolve_diy_price_id() self-provisions/validates: uses STRIPE_DIY_BOARD_RECRUITMENT_497_PRICE_ID if it is a valid active one-time $497 USD price, otherwise finds-or-creates by lookup_key diy_board_recruitment_497 (product tax_code txcd_10000000) and caches — so production with its LIVE key auto-creates the live price on first DIY checkout even if the env var holds a test id or is empty. Verified in preview: valid env id used; bad/absent id self-heals to lookup price.
- .gitignore: removed .env/.env.*/*.env lines (deployment blocker per deployment_agent); re-check PASSED.
- Deployment itself requires the OWNER to click Deploy/Update in the Emergent UI (agent cannot deploy). Production env settings should carry DIRECT_RECRUITMENT_VIDEO_URL.




## Funnel + App Presentation Cleanup (June 2026) — Complete, iteration_21 pass (1 defect fixed + re-verified)
- Shared Recruitment app: removed all visible "$497" (CoursePages PRODUCT_META label + ResultsPage back link → "Board Recruitment — Self-Guided System"; DashboardPage tier → "Self-Guided System"). Canonical six module titles untouched. Module pages now set document.title "Step N | <canonical title> | Nonprofit Board Builder"; overview title "Board Recruitment | Nonprofit Board Builder".
- /about-rooney: fully centered (stacked centered hero); subtitle now EXACTLY "Helping nonprofits build strong fundraising board." (replaced founder-story mission line); testimonial heading removed (TestimonialCarousel now renders h2 only when heading prop passed); carousel + offer cards intact.
- /recruit-your-board-yourself: centered; hero title+subtitle only; "You will receive the instructions..." paragraph moved as first paragraph of "You Don't Have to Figure Out the Process Yourself"; body text darkened (.brp-section p → var(--ink)); added "100% Money-Back Guarantee" + "What Happens After You Make Payment" (exact copy) before CTA; canonical TestimonialCarousel (heading "See What Other Nonprofit Founders and Leaders Have Said", idPrefix dyi) below buttons.
- /board-recruitment-proposal: centered (.brp-section text-align center; .brp-step-card single-column centered); added "What Happens After You Make Payment" (exact first-session copy) before final CTA; prices/CTAs/testimonials/icon-lists preserved (icon lists inline-block left-aligned within centered sections).
- seo.js: usePageMeta gained optional 3rd param noindex (adds/removes meta robots noindex — used ONLY by intake page); PAGE_META updated per user's exact copy for aboutRooney, recruitYourBoardYourself, boardRecruitmentProposal, boardRecruitmentIntake, recruitWithRooney ("Recruit With Rooney | Nonprofit Board Builder").
- Test member for module checks: module-tester@example.com / ModuleTest123! (entitlement recruitment_self_guided; in test_credentials.md).
- LEARNING: during a credits-exhausted interruption two "successful" search_replace edits silently reverted (CoursePages label, .brp-section CSS) — re-grep edited files after interruptions.


## Module Training Videos Connected (June 2026) — Complete, verified
- Six YouTube videos configured via EXISTING mechanism (db.course_videos via PATCH /api/admin/course-videos, admin JWT): M1 Crzh5tPpQYo, M2 671KaVEJFbg, M3 E4S16dkgKSY, M4 WxGpi3hHEO8, M5 -gbn1_sl9h8, M6 fdjjsiEnfWc — set for BOTH products (recruitment_self_guided + recruitment_basic; same canonical six modules).
- IMPORTANT: videos are DATA, not code — configured in BOTH environments: preview DB and PRODUCTION (via https://nonprofitboardbuilder.com/api admin login, all 12 PATCHes 200). No deploy needed for this change.
- Verified in preview logged-in (module-tester@example.com): all 6 embeds correct, placeholders gone, canonical titles intact, 16:9 responsive, no mobile overflow. No code changed.
- NOTE from user: materials generated inside the Recruitment app will be RESTRUCTURED in a SEPARATE upcoming task (user not satisfied with some generated materials — do not touch until instructed).


## Recruitment Funnel Update — 3 Batches (June 2026) — Complete, iteration_22 100% pass (17/17 backend + full frontend E2E)
- BATCH 1 (public pages/video/pricing): /recruit form reduced to 5 fields (name, email, organization, phone, "How Many Board Members Do You Want to Recruit?" select 1-6+/Not Sure); submit → /recruit-with-rooney (redirectAfterSubmit "rooney"; /recruit/process preserved for old links). Backend relaxed: funnel_models city/state/country optional; REQUIRED_ANSWERS recruitment = {new_members_needed}; present/active validation only when provided (reactivation/activation untouched). /recruit-with-rooney = training/decision page: full video li9NzvRCCEw (DIRECT_RECRUITMENT_VIDEO_URL env — PRODUCTION env value must be updated at deploy), NO $997 offer/checkout (rooney-checkout endpoint remains but unused publicly; RECRUIT_WITH_ROONEY_997_LIVE gate still off), two offer nav cards (DIY $497 → /recruit-your-board-yourself; "Recruit Your Board With Me" crossed $3,997 → $1,997 → /board-recruitment-proposal). Public price $1,998.50 → $1,997 everywhere (/about-rooney, proposal TODAY line, intake blocked card, owner-email label); "50% discount" wording removed ("offering you a discount"). DIY after-payment copy: intake → start immediately, launch in 30 minutes (NO Calendly/one-on-one).
- BATCH 2 (payments): generic resolve_offer_price_id(env_key, lookup_key, product_name, amount) validates configured env price (active, USD, one-time, exact amount) else find-or-create by lookup_key with tax_code txcd_10000000. DIY = 49700 (lookup diy_board_recruitment_497); direct project NOW 199700 via NEW lookup_key direct_board_recruitment_project_1997 (old $1,998.50 env price auto-rejected → live $1,997 price self-provisions in production on first checkout). Transaction amount 199700. setup_stripe.py catalog updated. Verified checkout session amount_totals 49700/199700.
- BATCH 3 (DIY journey): pay $497 → /purchase/success (account) → /board-recruitment-intake → submit → NEW /recruitment-start-here (member-protected via useMemberAuth, redirects /login?next=/recruitment-start-here; AuthPages already supported ?next). Intake submit returns redirect_url conditional on purchase_source (DIY → start-here, DWM → Calendly unchanged); intake done-card copy conditional. Start Here page (funnels/RecruitmentStartHerePage.jsx, MemberShell): hero "You're Ready to Start Recruiting Your Board", 5 exact instruction blocks, CTA "START STEP 1 — IDENTIFY THE BOARD MEMBERS YOUR ORGANIZATION NEEDS" → existing Module 1. Owner intake email next-step line conditional. Intake continues merging into recruitment_profiles (AI generation context) — prompts NOT rewritten (replacement prompts coming from user separately).
- NO new email automation. Historical $997 customers/records untouched.
- PRODUCTION deploy needs: user clicks Deploy; ensure production env DIRECT_RECRUITMENT_VIDEO_URL=https://youtu.be/li9NzvRCCEw; live $1,997/$497 prices self-provision.

## Five-Batch Recruitment Restructure (June 2026) — COMPLETE, verified iterations 23-27
- Batch 1: Six modules -> FIVE steps (Identify / Launch Campaign / Select & Interview / References & Background / Onboard). Strategy module retired (data preserved, progress archived module_number 99 via five_step_migration.py startup migration — runs in prod on deploy). Intake = permanent org memory (merge + organization_prefill); Module 1 ask-once with known-info summary + missing-only fallbacks; publish readiness no longer needs strategy.
- Batch 2: Module 1 blueprint count == new_members_count (Not Sure -> recommended count), spec profile structure; Module 2 auto Board Application + exactly 5 campaign generators (job post, LinkedIn, email, social, referral — CAMPAIGN_STANDARD prompts, real apply URL auto-inserted) + Launch My Recruitment Campaign + Continue to Step 3.
- Batch 3: Module 3 rebuilt — hosted applicants auto, external applicant (Name+CV required), Interview Invitation / Before-Interview Rejection (-> Not Moving to Interview) / candidate-specific Interview Guide (no scores) / Mark Interview Complete / decision-neutral After-Interview Thank-You; founder-controlled sends w/ recipient shown; auto guide generation on application removed.
- Batch 4: References — candidate form, referees land 'Ready to Contact' (NO auto-email), founder 'Email Reference for Confirmation', exactly 5 referee questions + declaration + explanation, founder notifications (References Submitted / Reference Received); network launch email rewritten (Board Leadership Opportunity | Org, Rooney voice, [Owner Preview] instead of [TEST MODE]); conditional_offer + org overview/manual/agreements prompts to spec.
- Batch 5: Module 5 — Onboarding Facilitator Guide prompt, persisted First Board Meeting details (PUT/GET /workspace/first-meeting; meeting id/passcode/chat optional), first meeting invitation w/ conditional profile reminder; My Board page (results route retitled) + NEW board_member_engagement_guide per joined member; $497 page rewritten outcome-driven (12 outcomes, guarantee, 30-min launch claim, support sections). Payments verified: $497 (49700) / $1,997 (199700) unchanged.
- NOT DEPLOYED YET: owner must click Deploy -> Update existing deployment; migration runs automatically at prod startup.
- Non-code issue: YouTube video li9NzvRCCEw on /recruit-with-rooney is set Private on YouTube — owner must make it Public/Unlisted.

## Production Verification (June 2026) — FAILED, 3 owner-action blockers (NO code defects)
- Production https://nonprofitboardbuilder.com is running the STALE iteration_22-era build: PUT /api/workspace/first-meeting = 404 on prod (401 in preview), /recruit-your-board-yourself missing all Batch 5 content. Five-batch restructure NOT live. Owner's recent "update" did not ship this fork's code. Deployer agent refused agent-dispatched deploy ("replace_via_ui_only") — OWNER must use the UI Deploy panel from THIS session and choose "replace existing app". Note: deployer debug was mis-scoped ("v3 deployment not found" for deploy_job_id 24abe3f2-...).
- Production Stripe is in TEST mode: /api/payments/diy-checkout and direct-project-checkout return cs_test_ sessions. Production uses a managed secret store — repo .env values NEVER overwrite existing prod secrets. Owner must set STRIPE_SECRET_KEY=sk_live_... and STRIPE_MODE=live in Deployment Panel → Secrets tab, then redeploy.
- DIRECT_RECRUITMENT_VIDEO_URL empty on prod ({"video_url":""}) — owner must set https://youtu.be/li9NzvRCCEw in Deployment Panel → Secrets tab (repo .env has it but won't propagate if the key already exists in prod store). Video also still Private on YouTube.
- What DID pass on current prod build: no public $997 offer, $1,997 + crossed $3,997 pricing correct, no $1,998.50 anywhere, $497 CTA clickable and creates checkout, exact guarantee sentence present, testimonials present, admin login works, DIY/DWM routing links correct.
- AFTER owner completes the 3 actions: rerun the SAME full production verification once (user's checklist in this session) via testing_agent against https://nonprofitboardbuilder.com — read-only, NO completed payments, NO real emails.
- Copy change (June 2026): /board-recruitment-proposal "Project Summary" heading → "Executive Summary" (verified in preview; ships with the pending redeploy).
- SECOND verification attempt after owner's "update": production STILL identical stale build (same bundle main.a7013456.js, first-meeting 404, cs_test_ Stripe, empty video URL). Deployer debug CONCLUSIVE: this session's deploy-job-id (24abe3f2-d733-4467-8dd0-c92e5ebad863) resolves to NO v3 deployment; the deployment owning nonprofitboardbuilder.com lives in a DIFFERENT session/fork lineage — owner's update never reached it. Agent-dispatched deploy blocked ("replace_via_ui_only"). OWNER must: Deploy panel from THIS session → replace existing app (the one whose Domains tab lists nonprofitboardbuilder.com), or re-point the custom domain; then set Secrets: STRIPE_SECRET_KEY=sk_live_..., STRIPE_MODE=live, DIRECT_RECRUITMENT_VIDEO_URL=https://youtu.be/li9NzvRCCEw. Rerun full verification (testing_agent, read-only, no completed payments) once the new build is live.

## Later June 2026 session (post-deploy fixes/features)
- Five-batch build CONFIRMED LIVE on production (new bundle, Batch5 API 401, Executive Summary + Batch5 content rendering). Video public on YouTube. Owner reported Stripe production keys now LIVE. DIRECT_RECRUITMENT_VIDEO_URL still needs owner to set in prod Secrets (was empty at last check).
- /about-rooney: added "Meet Rooney Akpesiri / The Nonprofit Board Builder" founder story below the two offers + "Want to Discuss Your Board With Me?" CTA (BOOK A CALL WITH ROONEY → https://calendly.com/boardbuilder/recruitboard, the existing public booking URL). Screenshot verified.
- Public Log In links added: homepage nav (nav-login-link), funnel nav (funnel-login-link incl. restrained pages), funnel footer (footer-login-link) → /login.
- DWM $1,997 account experiment REVERTED per user correction: flow stays payment → /board-recruitment-intake (no account) → Calendly. Claim rejects direct_board_recruitment_project. Verified iteration_29 (100%).
- "Before You Start" section removed from legacy /recruit/checkout (only page that had it). Verified iteration_29.
- $497 account journey fully verified E2E (iteration_28, 100%): payment → /purchase/success account create/reuse → intake → start-here → Modules 1-5 → logout/login persistence; duplicate-account prevention; payment security (fake/unpaid/foreign session claims rejected); tenant isolation.
- RECRUITMENT LEAD NURTURE V2 (iteration_30, 42/42 + 37/37 legacy): replaced 4 stale templates with 3 approved sales emails — E1 "Before You Decide How to Build Your Board"→/about-rooney (Rooney story, exact preserved sentence), E2 "You Can Build Your Board Yourself"→/recruit-your-board-yourself ($497), E3 "Want Me to Help You Build Your Board?"→/board-recruitment-proposal ($1,997). Rotation 1→2→3→1 indefinite, Tuesday 7AM ET, duplicate-week protection, failed sends don't advance. Sender for recruitment: "Rooney Akpesiri | The Nonprofit Board Builder <nonprofits@...>". New env RECRUITMENT_LEAD_NURTURE_ENABLED=true (LEAD_NURTURE_ENABLED stays false → Reactivate/Activate remain off). enabled_nurture_sources()/nurture_sender() in marketing_service.py. Purchase stop at VERIFIED payment (not claim): stop_recruitment_nurture_for_purchase called from payment_status GET + Stripe webhook (paid only); claim-time stop kept as fallback; idempotent, never fails payments. SAFETY: real broadcasts skipped when DB_NAME==test_database (preview guard — intentional; prod DB differs). Resend TRIPLE-brace merge {{{FIRST_NAME|there}}} confirmed correct per official docs (testing agent 2-brace claim was false positive; tests fixed).
- DEPLOY NEEDED: nurture v2 + about-rooney + login links + checkout cleanup exist only in preview until owner redeploys (new env key RECRUITMENT_LEAD_NURTURE_ENABLED will propagate as a new secret on deploy).

## Board Reactivation Build (June 2026 fork — PREVIEW ONLY, not deployed)
- Batch 1 (funnel /reactivate → /reactivate-with-rooney, $497 DIY + $1,997 DWM Stripe checkouts, 5-step intake, account claim entitlement reactivation_self_guided, 5-step app shell): BUILT + VERIFIED (iteration_31, PASSED).
- Batch 2 (Step 2 roster, secure Board Member Profile & Recommitment forms /board-recommitment/:token, send/reminder/call-script/resend, founder notifications, response view, admin overview): BUILT + VERIFIED (iteration_32, PASSED).
- Batch 3 (Step 3 conversation scripts via Claude, Conversation Conclusion + founder-selected Outcome with org-permitted transitions, materials Edit/Save/Approve/PDF): BUILT + VERIFIED (iteration_33, PASSED except AI-content spot-check blocked by LLM budget at the time — one clean regeneration still to validate; delete stale 25-char material first).
- Batch 4+5 (Board Member Portfolios generated/managed from Step 5 My Board per Architecture Correction; secure /portfolio/:token + B/W bordered PDF + deterministic portfolio email w/ auto link; My Board dashboard groups + cross-sell to /recruit and /activate; reactivation accountability service (day 2..21 progress-aware, enroll on $497 claim); non-buyer nurture: 3-template weekly rotation, flag REACTIVATION_LEAD_NURTURE_ENABLED=false, purchase-stop for both reactivation sources): BUILT, NOT YET VERIFIED by testing agent.
- QUEUED (specs in /app/memory/reactivation_build_notes.md): Cross-Product Architecture Correction (recruitment 6 modules + recruitment My Board portfolios + homepage diagnostic CTA + /board-transformation + About Rooney 3 offers), Activation Batches 1-4 (ALL FOUR SPECS TRUNCATED in transmission — need re-send: B1 from Part 6 §37, B2 from Part 7 §34, B3 from Part 6 §30, B4 from Part 7 §34/Module 5).
- Key files: backend reactivation_routes.py, reactivation_intake_routes.py, reactivation_accountability.py, ai_service.py (3 new generation types), marketing_service.py; frontend member/ReactivationStep2/3/5.jsx, ReactivationCoursePages.jsx, funnels/Reactivate*.jsx, BoardReactivation*.jsx, pages/PortfolioPage.jsx.

## 2026-06 — Final Reactivation Verification (Batch 4+5 + AI check) — PASSED
- iteration_34: Batch 5 spec reconciliation verified (28/28 pytest). 2 fixes: accountability portfolios CTA 'GO TO MY BOARD'; version-safety (edited-after-send portfolio returns to Draft in my-board summary, requires re-approval, sent history preserved).
- iteration_35: Difficult Conversation AI content check PASSED (13.3k-char person-specific script). Batch 4 Step 4 data handoff PASSED. ONE blocking defect: conversation-script generation ~66s > 60s ingress timeout -> external 502.
- iteration_36: FIX verified — POST /conversation-script now async: returns {material_id, status:'Generating'} in 0.1s, background asyncio task fills content -> Draft (~65s), Failed status on error, duplicate-guard. Frontend ReactivationStep3.jsx polls every 4s, resumes on mount, gates buttons, shows retry note on Failed. 52/52 tests pass.
- Missing video URLs (content task, not code): reactivation sales video (DIRECT_REACTIVATION_VIDEO_URL) + Steps 1-4 course videos (5 slots total). Step 5 needs no video.
- Complete Reactivation Build: PASSED. Next: Activation Batches 1-5 (specs still needed from user, previous messages truncated).

## 2026-06 — Cross-Product Architecture Correction — BUILT + VERIFIED (iterations 37-39)
- Recruitment remapped 5→6 modules: new training-only M1 'Recruiting Board Members the Right Way' (placeholder 'Board Recruitment Training Video Coming Soon'); old M1-5 → M2-6 with videos; collision-safe startup migration six_module_recruitment_v1 (progress/videos/materials shifted, temp-offset 200); accountability stages remapped to 6 modules (engine unchanged); course_routes validators le=6 (blocking defect found+fixed by tester in iter37).
- Recruitment My Board portfolios: approve mints share_token; secure /portfolio/{token} + black/white PDF (shared reactivation infra, portfolio_member_name generalized); deterministic portfolio-email endpoints GET/POST /api/workspace/applications/{id}/portfolio-email (auto link, review/edit, send→SENT+sent_version); version safety (edit→409 until re-approve, sent_version preserved). Full e2e verified.
- Universal diagnostic /board-transformation: contact + exactly 4 steps, deterministic rules (recruit: Yes/Not Sure; reactivate: total>active AND positive answer; activate: current fundraising weak AND want=Yes), order reactivate→recruit→activate; result page /board-transformation/result/:token ('Your Board Transformation Journey', framework, 'Based on What You Told Us', 'Which Would You Like to Do First?' 3 cards → *-with-rooney); select endpoint persists selected_product; NO nurture enrollment for board_transformation (sync_lead_nurture guard).
- Homepage hero universal CTA 'TELL ME WHAT MY BOARD NEEDS' → /board-transformation. About Rooney: diagnostic CTA + three offers (recruit/reactivate/activate-with-rooney), story preserved. /activate-with-rooney placeholder page (no funnel/payment — Activation NOT built).
- Activation baseline for future build: 5 modules + My Fundraising Board (not module 6), Fundraising Portfolio distinct from Board Member Portfolio.
- Test reports: iteration_37 (full checklist, 51/51), iteration_38 (fix verification), iteration_39 (portfolio UI slice, all pass, seed data cleaned).
- NEXT: Activation Batches 1-5 (await specs from user). Reactivation video URLs + new Recruitment M1 video still placeholders (content task).

## Activation Batch 1 — COMPLETE & VERIFIED (June 2026, iterations 40-42)
Built per the authoritative reconciled spec (Sections 1-78 + Parts 12-15 + 140-point verification):
- Public funnel: /activate short lead form (name/email/org/phone/board_member_count select, source fundraising_activation, CTA "SHOW ME HOW TO ACTIVATE MY BOARD") → /activate-with-rooney. Board Transformation visitors go directly to /activate-with-rooney (no re-capture); no activation nurture (Batch 5).
- /activate-with-rooney: real choice page replacing placeholder — video slot GET /api/direct-activation/config (DIRECT_ACTIVATION_VIDEO_URL empty → placeholder "Board Fundraising Activation Video Coming Soon"), DIY "Activate Your Board Yourself" $497 → /activate-your-board-yourself, DWM "Activate Your Board With Rooney" $2,497 → /board-activation-proposal, exact guarantee "Your investment is protected by our 100% money-back guarantee.", canonical TestimonialCarousel.
- DIY sales page /activate-your-board-yourself: $497 one-time, "Stop Carrying Fundraising Alone", "People who plan together execute together", 10 transformation outcomes, CTA "MAKE PAYMENT AND START ACTIVATING MY BOARD" → POST /api/payments/activation-diy-checkout.
- DWM proposal /board-activation-proposal: Executive Summary, $2,497, CTA "MAKE PAYMENT — LET'S ACTIVATE YOUR BOARD TOGETHER" → POST /api/payments/activation-project-checkout.
- Payments: DIY source direct_diy_board_activation_497 → entitlement activation_self_guided on claim at /purchase/success → routes to /board-activation-intake → intake submit → /activation-start-here. DWM source direct_board_activation_project_2497 (NO entitlement, NO account) → /board-activation-intake → Calendly https://calendly.com/boardbuilder/recruitboard. Verified-payment only, idempotent, failed/cancelled = no access. Stripe lookup keys: diy_board_activation_497 (49700), direct_board_activation_project_2497 (249700).
- /board-activation-intake: 5-step intake capturing all Section 30-48 fields (fundraising_goal, amount_needed + Not Sure checkbox, money_accomplish, current_methods multi, written_strategy, fundraising_calendar, fundraising_carriers multi, present/active board counts with active<=total validation, involvement, activities multi, barriers, skills, direction_12_24, priorities, prior planning x2, desired_change, success_definition, anything_else). Collection board_activation_intakes (unique session_id), prefill from member + recruitment_profiles, upsert-idempotent, owner Resend email once, no AI.
- /activation-start-here (exact Section 52 copy, CTA "START ACTIVATING MY BOARD") + 5-module app /app/activation/self-guided: Module 1 "Building a Fundraising Board" training-only; Modules 2-5 shells (exact titles); module 6 route = Not Found, progress API 422 for module>5; My Fundraising Board preview card = post-module dashboard, NOT Module 6. Videos configurable via admin course-videos product=activation_self_guided modules 1-5, all placeholders "Board Fundraising Activation Training Video Coming Soon".
- Key files: backend activation_intake_routes.py (NEW), payment_routes.py, member_routes.py (claim ladder + dashboard entry + labels), course_routes.py, course_content.py (ACTIVATION_MODULES), review_routes.py, funnel_service.py, server.py, .env (3 new empty keys); frontend Activate* pages, BoardActivationProposalPage, BoardActivationIntakePage, ActivationStartHerePage, ActivationCoursePages, funnelConfig.js, FunnelStepForm.jsx, PurchaseSuccessPage.jsx (activation routing + generic subheading), DashboardPage.jsx (per-product labels), App.js, seo.js.
- Testing: iteration_40 (one blocker: missing claim ladder branch — fixed), iteration_41 (full rerun PASS incl. real 4242 test-mode e2e purchase→claim→intake→start-here→modules; pytest 60/60 incl. new tests/test_batch1_activation.py 29/29), iteration_42 (dashboard labels browser-verified; PurchaseSuccessPage routing verified via code + API contract claimed_source=direct_diy_board_activation_497; 29/29 pytest). Regression: recruitment 6-module, reactivation, diagnostic, auth all preserved.
- NOT built (later batches, intentional): Module 2 planning form (Batch 2), Module 3 strategy generation/review (Batch 3), Module 4-5 tools (Batch 4), My Fundraising Board finished dashboard + Fundraising Portfolios + activation DIY accountability + activation prospect nurture (Batch 5). No activation videos supplied. Production NOT deployed.
- NEXT: await user's Activation Batch 2 spec (Module 2 — Fundraising Planning Form).

## Activation Batches 2-5 — BUILT (June 2026); Batches 2-3 VERIFIED, Batches 4-5 CODE COMPLETE (testing pending user authorization)
- Batch 2 (VERIFIED iteration_43): Module 2 "Initiating the Planning Process" — Fundraising Planning Master Form generation, secure tokenized participant links for board members (no accounts), original response tracking in activation_engagements, founder response view. Backend activation_planning_routes.py; frontend ActivationModule2.jsx.
- Batch 3 (VERIFIED iteration_44): Module 3 "Strategy Generation & Review" — async background Claude generation of the Fundraising Strategy Plan (poll until Draft, no 502s), board review workflow via secure tokenized links (StrategyReviewPage.jsx), review submission tracking.
- Batch 4 (BUILT, untested): Module 4 "Facilitating Plan Adoption" — Facilitation Guide generation, manual adoption status updates, responsibility assignment per board member. Module 5 "Equipping Board Members" — Fundraising Toolkit generation. Frontend ActivationModule4.jsx / ActivationModule5.jsx.
- Batch 5 (BUILT, untested): My Fundraising Board dashboard (MyFundraisingBoardPage.jsx), Individual Fundraising Portfolio generation + secure /fundraising-portfolio pages (FundraisingPortfolioPage.jsx), Activation DIY 21-day accountability (activation_accountability.py, enrolls on $497 activation claim), Activation prospect nurture (flag ACTIVATION_LEAD_NURTURE_ENABLED=false — OFF).
- USER COMMAND: "BUILD EVERYTHING FIRST. DO NOT TEST YET." — no testing agent run for Batches 4-5; awaiting user's final one-shot complete Activation test authorization + checklist.
- Mocked/pending: all Activation videos are placeholders; ACTIVATION_LEAD_NURTURE_ENABLED=false; production NOT deployed.

## Code Review Fixes (June 2026) — Applied, behavior-preserving only
- REAL BUG FIXED: marketing_service.py NURTURE_TEMPLATES defined "fundraising_activation" twice — stale 4-template block (retired /activate/options URLs) silently overrode the Batch 5 Rooney-voice 3-template set. Stale block deleted; correct templates (about-rooney / $497 DIY / $2,497 DWM) now active (flag still false).
- Secrets: hardcoded admin password removed from 15 tests/*.py + backend_test_phase4.py → os.environ["ADMIN_PASSWORD"]; new tests/conftest.py loads backend/.env (+frontend/.env for REACT_APP_BACKEND_URL fallback); pycache purged. Password now only in .env + memory/test_credentials.md.
- Dead code: unused `reviews` var removed (activation_accountability.activation_stage); shadowed `origin_of` import removed (workspace_routes.py).
- FALSE POSITIVES (verified, not changed): flagged `is` comparisons are `is not None` (correct); no literal `is` comparisons in production code; pyflakes full scan found zero undefined variables.
- DEFERRED per user's standing "do not redesign / no testing" order: refactor of create_activation_planning_router (1,171 lines) + other high-complexity functions, import reorganization, test-file `is True` style. Revisit only after owner's manual Activation testing.
- PRE-EXISTING (untouched): tests/test_nonprofit_board_builder_automation_e2e.py fails collection (imports removed automation_service.applicant_email — stale legacy test).
- Verified via compile/import checks + backend health only (no e2e/emails/payments, per user's stand-by order).

## Code Review Round 2 (June 2026) — Applied, behavior-preserving only
- Remaining test-password literals moved to env: backend/.env got TEST_ACCOUNT_PASSWORD ("TestPass123!"), ACTIVATION_TESTER_PASSWORD ("ActTest123!"), MODULE_TESTER_PASSWORD ("ModuleTest123!") — values preserved so persistent tester accounts (activation-tester, module-tester@example.com) still authenticate. All tests/*.py literals replaced with os.environ[...] (conftest.py loads .env). Zero hardcoded passwords remain in code.
- Unused imports removed from production files (html, Optional, List, timezone, Iterable, run_interview_guide, KeepTogether at refinement_routes:654 only — the line-574 PDF builder still uses KeepTogether); dead locals removed (objective in public_opportunity_routes, OUTCOME_FOLLOW_UP in reactivation_routes); f-string-without-placeholder fixed (opportunity_emails). five_step_migration.py got type hints (AsyncIOMotorDatabase -> None). Production *.py now pyflakes-clean.
- FALSE POSITIVES again (unchanged): workspace_routes `is` lines are `is not None` (identity check, compliant with the review's own rule); test `is True/is False` also compliant; zero undefined variables found by full pyflakes scan.
- STILL DEFERRED (cannot verify without the regression runs the user paused): create_activation_planning_router split + other complexity refactors, facade/import restructuring, test-file type hints.
- Verified: pyflakes clean (production), backend restart + health 200, pytest --collect-only 546 tests (1 pre-existing stale-test collection error unchanged). No e2e/emails/payments run.

## Activation FINAL Functional Test (June 2026) — PASSED, iteration_45 100%
- ONE authorized end-to-end test of the full Activation journey (Modules 2→5 + My Fundraising Board + Fundraising Portfolio) using the retained tester activation-tester-1786708997@example.com. Backend pytest 4/4 (Batch 4 adoption+toolkit e2e w/ 2 real Claude generations; Batch 5 my-board+portfolio e2e w/ 1 real Claude generation; accountability config; nurture config). Frontend: all public activation pages + start-here + module shells + My Fundraising Board render correctly.
- Verified: Module 4 guide (13 headings, org-specific, no invented bylaws/legal, AI never assigns responsibilities), all 3 plan_status paths ('Further Review Needed' blocks Module 5 w/ 409; 'Adopted With Changes' finalize preserves reviewed vs adopted text), founder-recorded responsibilities/statuses, Module 5 toolkit (call scripts, no invented donors), My Fundraising Board counts/access, Alice portfolio (exact agreed responsibility, her own data only, B/W bordered public page + PDF, secure token no-login, invalid token 404), Carol (Follow-Up Needed) portfolio + email-preview correctly 409-blocked, prepared emails verified WITHOUT sending, accountability DIY-only ($2,497 DWM excluded, progress-aware 9 stages), nurture OFF (flag false, 3-template rotation, purchase-stop both sources, board_transformation not enrolled).
- ZERO defects. One non-blocking note: GET /activation/members/{id}/portfolio doesn't expose fp_share_token directly (UI reads link from email-preview — works as designed).
- NO emails sent, NO payments rerun, NO other product suites, NO deploy.
- Tester account end state: 'Adopted With Changes' finalized, toolkit Approved, Alice portfolio Approved; test edit markers [TEST_BATCH4_MARKER_EDIT]/[TEST_BATCH5_FP_MARKER] remain in guide/portfolio drafts (cleanup deferred per no-extra-cleanup order). New regression file: tests/test_batch5_activation_myboard_portfolio.py.
- READY for Rooney's manual Preview review. Production NOT deployed.

## Deployment Blocker Fix (June 2026) — committed ca95f6b
- Prod deploy failed: old committed six_module_migration crashed on dup key (course_videos 205, half-migrated prod state, repeated startup crashes). REWRITTEN hardened migration: guard six_module_recruitment_v1 claimed status=running BEFORE mutation, completed on success (legacy no-status guard = completed, preview no-op); self-healing course_videos deterministic rebuild (deletes strays incl. 200+ temp rows, canonical modules 2-6 from FIVE_STEP_VIDEOS, module 99 preserved); course_progress legacy-aware reconstruction + six_module_done flags + duplicate-merge (no double-shift ever); materials flag-guarded descending shift excl. reactivation_/activation_ types. Verified via scratch-DB simulations of the exact crashed prod state (resume, no-op, custom-preserve, legacy-guard scenarios). Preview data fix: self_guided module 6 video restored. DO NOT modify.

## Admin Service Delivery + Content Centralization + Strategic Planning (June 2026) — BUILT, NOT TESTED (user order: build only)
- member_auth.py: review_mode_member now grants all 3 product entitlements (Admin Test Product Journey); NEW operator_member — admin cookie + X-Operate-As header → operates members docs flagged operator_workspace ONLY (real customers unreachable).
- admin_service_routes.py: GET /api/admin/dwm-clients (verified paid DWM transactions ×3 products joined w/ intakes + workspace status); POST /api/admin/dwm-clients/{session_id}/workspace (idempotent operator member creation, entitlement per product, stamps intake.user_id, NO accountability/nurture — created without claim). DWM_OFFERS maps the 3 project purchase_sources.
- strategic_planning_routes.py: full admin SP pathway — sp_projects/sp_participants/sp_forms/sp_plans collections; form upload (extract_reference_text) → Claude structuring (strategic_planning_form_structure, uploaded form is authority) → edit/approve versions; secure tokens /strategic-planning-form/{token} (+submit, owner notify); foundational plan async gen (strategic_planning_foundational, who-said-what) → edit/approve → review links /strategic-plan-review/{token} (4 choices/area + comments) → finalize (history preserved: draft_history, responses, reviews, finalized_text); areas w/ statuses NOT ASSIGNED..READY FOR BOARD, suggest-owners (deterministic alignment), owner assignment, pack async gen (strategic_area_pack) + /area-pack/{token} public pack + plan submission (submission_history) + owner notify; adoption recording; final plan async gen (strategic_final_plan) + edit/approve + PDF (build_portfolio_pdf). All sends founder/admin-triggered w/ email-preview endpoints; 4 new GENERATION_TYPES in ai_service.py (module 0).
- Frontend: api.js X-Operate-As interceptor (admin cookie flows via withCredentials); operatorMode.jsx (enter/exit + WorkspaceModeBanner: operator banner + TEST MODE banner via member.review_mode, mounted App.js); admin/ClientDeliverySection.jsx (Test Product Journey 3 links + DWM directory + Open Client Workspace); admin/StrategicPlanningSection.jsx (complete SP workflow UI w/ polling, editors, email-preview modal w/ explicit Send, reviews, areas, adoption, final plan + PDF); AdminPage tabs "Client Delivery" + "Strategic Planning"; public pages StrategicPlanningFormPage/StrategicPlanReviewPage/AreaPackPage + App.js routes.
- Content centralization: frontend/src/content/siteContent.js — verbatim-extracted headlines/CTAs/guarantees for 16 pages (3× with-rooney, 3× DIY, 3× proposal, 3× start-here, about-rooney, homepage CTA, board-transformation + result); pages import SITE_CONTENT. Funnel landing copy already centralized in funnelConfig.js; module titles in backend course_content.py; email templates in marketing_service/accountability/route builders (noted in file banner).
- NOT TESTED (user forbade all testing); frontend compiles clean, backend boots clean. Awaiting user's testing authorization.

## 2026-06 — Platform-wide content centralization COMPLETE (this session)
- appContent.js: 42 namespaces / 320+ entries; siteContent.js: 16 public pages (verbatim)
- 61 of 63 string-bearing frontend files consume canonical content (exclusions: BlogPages.jsx = blog data, LegalPage.jsx = legal docs)
- backend/content_templates.py now holds Reactivation + Strategic Planning static emails; course_content.py canonical for modules
- CONTENT_ARCHITECTURE.md documents the whole layer; no duplicate exports; all refs resolve; build + backend verified
- Also this session: Reactivation workflow correction (Steps 1-5 incl. Understand Their Response, direction scripts, bylaws intake, outcome emails, dashboard pipeline), Recruitment + SP corrections, prod build fix (AdminPage imports)
- NOT run per user instruction: functional/regression tests, emails, payments, deployment

## 2026-06: Cross-Offer Dashboard Link Correction (user-approved, exactly 4 edits)
- MyFundraisingBoardPage.jsx: RECRUIT NEW BOARD MEMBERS -> /recruit-your-board-yourself; REACTIVATE MY BOARD -> /reactivate-your-board-yourself
- ReactivationStep5.jsx: RECRUIT NEW BOARD MEMBERS -> /recruit-your-board-yourself; ACTIVATE MY BOARD -> /activate-your-board-yourself
- Authenticated dashboard cross-offers now land on $497 DIY sales pages, never /recruit, /reactivate, /activate lead-capture funnels
- Untouched per instruction: public landing/funnel links, admin review link, payments, intake routing, auth, entitlements
- Verification: static only (routes confirmed in App.js lines 92/97/106; frontend compiling, service RUNNING). No functional testing per standing user command.

## 2026-06: Activation Modules 2-5 Workflow Correction (user-approved decisions: shared links A, per-idea review A, responsibilities in Module 5 A)
- Module 2: initiation only — generate/edit/approve form + GENERATE EMAIL with shared secure form link (shared_form_token on activation_planning_forms); board members self-identify on /planning-form/{token}; participant matched/created by email; removed Add Board Member UI, suggestions import, per-person send/reminder/call-script UI, GO TO STEP 3 CTA; kept Responses Received list + response modal (notification deep-link preserved)
- Module 3: generate strategy + GENERATE EMAIL with shared review link (shared_review_token on activation_strategies); removed reviewer roster + GO TO MODULE 4 CTA; review notification now deep-links to module/4
- Public review (/strategy-review/{token}): supports shared token with self-identification (name/email/role) + per-idea Approve/Disapprove with required reason on disapprove (idea_reviews in review dict, ideas parsed from plan sections via parse_plan_ideas); existing overall questions kept; per-participant tokens still work
- Module 4: Board Review Participants (positions + idea decisions), GENERATE FUNDRAISING STRATEGY (revised synthesis, prompt activation_revised_strategy, stored revised_* on activation_adoptions), meeting details form (PUT /activation/adoption/meeting), GENERATE ADOPTION MEETING EMAIL (plan_share_token, public /strategy-plan/{token} + StrategyPlanPage.jsx), facilitation guide kept (context now uses revised plan when present), conclusion kept (transcript/notes copy), plan status kept (adopts revised text when present); Responsibilities section REMOVED
- Module 5: Equip Each Board Member — responsibility recording moved here (same PUT responsibility endpoint), per-member GENERATE FOLLOW-UP EMAIL (prompt activation_followup_email, followup_* on participant) with edit/copy; toolkit unchanged; dashboard button added underneath (canonical route + activationContent.overview.previewButton)
- My Fundraising Board: unchanged except responsibilities link now points to Module 5; portfolio gating/architecture preserved
- Preserved: all existing AI prompts, per-person secure links/data, Resend send endpoints (backend intact), payments, entitlements, auth, tenant isolation
- Verification: py_compile OK, backend boot OK, webpack compile OK, CI=true prod build OK, route smoke curls (404/401 as expected). No functional testing/emails/payments/deploy per user instruction.

## 2026-06: About Rooney Page + Platform-Wide Testimonial Centralization
- About Rooney offer cards replaced with exact supplied copy (Recruit New Board Members / Reactivate Your Board / Build A Fundraising Board), now sourced from SITE_CONTENT.aboutRooney.offers in siteContent.js; buttons/destinations unchanged
- Green banner (ar-hero): added .ar-hero-copy .button { margin-top: 26px } for breathing room before "TELL ME WHAT MY BOARD NEEDS"
- Testimonials: canonical file MOVED verbatim (git mv, zero rewrite) from components/testimonialsData.js -> content/testimonials.js (8 records + initialsFor); TestimonialCarousel.jsx + TestimonialsSection.jsx imports updated; no other testimonial copies exist anywhere (grepped)
- Verified: exact copy rendering on /about-rooney via screenshot, carousel working, CI prod build OK
- PENDING (queued by user, NOT started): FIRST BUILD INSTRUCTION — homepage redesign (single TELL US ABOUT YOUR ORGANIZATION CTA + supplied hero/intro/how-we-help copy), 5-question board assessment visual redesign, central board transformation video page, 3 individual offer video pages with DIY $497 + DWY payment choices beneath video, purchase routing (DIY -> post-payment intake -> Module 1; DWY -> scheduling + Admin DWM visibility), NEW Recruitment Module 1 "Understanding the Board Recruitment Process" (current M1 content shifts to M2 "Identifying the People Your Board Needs"), single next-module navigation, full content centralization. Existing funnel today: /board-transformation (assessment) -> BoardTransformationResultPage; recruitment modules defined in backend/course_content.py (currently M1 "Recruiting Board Members the Right Way", M2 "Identify the Board Members Your Organization Needs", 6 modules total)

## 2026-06: Homepage + Board Assessment Funnel + Recruitment Module 1 (FIRST BUILD INSTRUCTION)
- Homepage (LandingPage.jsx): hero replaced with exact supplied copy (title/subtitle/single CTA "Tell Us About Your Organization" -> /board-transformation); new Introduction + How We Help sections (exact copy, second CTA per PART 4); all content in SITE_CONTENT.home; preserved: 3-stage section, testimonials, discovery, three-button CTA sections, FounderStory (About Rooney + green box), blog slider, join-a-board, footer
- Assessment (/board-transformation): visual restyle via App.css (.bt-page/.bt-option rules — spaced options, black text, larger cards/typography); intro centralized (SITE_CONTENT.boardTransformation.intro, "five questions"); logic untouched
- Central video page: BoardTransformationResultPage rebuilt video-first (video-stage placeholder, 3 offer buttons from SITE_CONTENT.boardTransformationResult -> /offer/recruitment|reactivation|activation, TestimonialCarousel beneath, no info re-entry); token fetch + best-effort select POST preserved
- Offer video pages: new OfferVideoPage.jsx (routes /offer/*) — video placeholder + DIY $497 button -> existing DIY checkout pages (/recruit|reactivate|activate-your-board-yourself) + DO IT WITH YOU button -> existing DWY proposal flows (/board-*-proposal); canonical testimonials reused; content in SITE_CONTENT.offerVideos
- Payment/post-payment routing: UNCHANGED existing architecture (DIY: checkout -> post-payment intake -> Module 1; DWY: proposal/scheduling + DWM Client Directory admin visibility)
- Recruitment modules: course_content.py M1 retitled "Understanding the Board Recruitment Process", M2 "Identifying the People Your Board Needs" (both structures); appContent recruitmentContent.module1 heading/body updated, duplicate continue button REMOVED from CoursePages.jsx (standard module nav only); MaterialsLibrary MODULE_NAMES + accountability_service focus text synced. No module renumbering needed (content already in correct positions); no downloadable resource in M1
- Verified: CI prod build OK x2, backend boot OK, homepage/offer/assessment screenshots confirm exact copy + routing. NOT functionally tested per instruction
- Note: stale backend test assertions reference old M1 title (tests not run per instruction); reviewMode.jsx admin labels untouched (legacy numbering)

## 2026-06: Homepage Cleanup + Complete Homepage Content Centralization + CTA text change
- REMOVED 3 sections from LandingPage.jsx: "A practical path forward/Three Stages" (+stages array), "Clarity before action/Discover Exactly" (+discoveries array), "Start with the truth/Tell Us What Is Happening" (assessment-cta) — repo-wide grep confirms strings gone
- Preserved: final-cta three-button section (explicitly preserved), testimonials, FounderStory, blog, join-network, footer
- Removed dead nav links (How It Works, What We Help You Fix, Tools and Materials, Board Solutions) from homepage nav + JoinBoardPage nav (dead anchors after section removal); footer How It Works link removed
- FULL centralization: every homepage static string now in SITE_CONTENT (home.nav, home.heroCard, home.finalCta, home.joinNetwork, home.footer + existing hero/intro/how); TestimonialsSection headings -> SITE_CONTENT.testimonialsSection; FounderStorySection copy -> SITE_CONTENT.founderStory (paragraphs, credibility, labels). ~60 homepage-rendered strings audited. Testimonial text still from content/testimonials.js
- Single homepage implementation confirmed: App.js "/" -> HomeExperience -> LandingPage; no legacy/mobile variants; same canonical content across breakpoints
- CTA text changed (user request): "Tell Us About Your Organization" -> "Tell Us About Your Board" (single key SITE_CONTENT.home.cta, updates hero + How We Help buttons)
- Hero layout fix: .hero-copy max-width 710->1080 (border-box padding was squeezing content to 274px causing one-word-per-line title); h1 clamp reduced to 58px max
- Verified: desktop + mobile screenshots, removed headings absent from rendered body, CI prod build OK, backend boot OK. Not functionally tested per standing instruction

## 2026-06: Homepage Visual Correction + Video-to-Payment Routing + Recruitment M1 Completion Fix
- Recruitment Module 1 BUG FIXED: root cause = prior duplicate-button removal also suppressed standard Next nav for self-guided M1, leaving no path to M2. Now: mark-complete -> 'Step Completed' state + 'NEXT: IDENTIFY THE PEOPLE YOUR BOARD NEEDS' button (canonical key recruitmentContent.module1.nextButton) -> module 2. TESTED by testing_agent iteration_46: 100% pass (visibility gating, persistence, single button, M2 regression OK)
- Homepage: single green hero banner (kept green treatment, carries canonical title/subtitle/CTA; cream block + mission-card visual removed; heroCard content def deleted); intro grouped in white card box (.home-intro); final-cta subtitle REMOVED (title + 3 buttons only); testimonial subtitle REMOVED globally (siteContent.testimonialsSection.supportingText deleted + TestimonialsSection p removed)
- Offer video pages now route DIRECTLY to Stripe checkout (no $497 sales-page hop): recruitment diy-checkout/direct-project-checkout, reactivation reactivation-diy/project-checkout, activation activation-diy/project-checkout; DWY prices on labels ($1,997/$1,997/$2,497); checkout error string canonical (offerVideos.checkoutError); post-payment architecture untouched
- CONTENT AUDIT (honest): 603 inline multi-word JSX strings remain across 78 files (top: BoardRecruitmentProposalPage 44, ProductToolsSection 33, ApplicantModules 31, BoardRecruitmentIntakePage 27, ActivationModule4 22...). Blocking reason: prior migrations centralized headings/labels/canonical sections but left long-form instructional/sales paragraphs inline in interactive components. Full migration = large mechanical pass over 78 files, offered as next phase pending user approval (regression risk right after production deploy)
- Verified: CI prod build OK, backend boot OK, desktop+mobile renders, removed strings absent repo-wide + rendered-body

## 2026-06: FULL PLATFORM-WIDE CONTENT CENTRALIZATION (603-string migration)
- Codemod migrated 585 static user-facing strings (text nodes >=4 words + static placeholder/aria-label/alt/title attrs) from 75 files into per-page named exports appended to appContent.js (member/admin) and siteContent.js (funnels/pages/public/components); imports auto-wired; machine inventory at /app/memory/content_migration_inventory.json
- +2 manual migrations (WorkspaceModules launch note, JoinBoardPage experience note); 3 backend static activation email templates moved to content_templates.py (activation_planning_email, activation_review_email, activation_adoption_meeting_email, activation_signature) consumed by activation_planning_routes.py
- Collision handling: 7 pre-existing export names (adminPageText, materialsLibraryText, sharedPagesText, portfolioPageText, memberShellText, funnelLayoutText, funnelFormText) merged into the pre-existing canonical exports; duplicate imports deduped; 3 regex code-corruptions reverted (JS comparisons matched by >x< pattern)
- FINAL AUDIT: 0 unexplained user-facing strings (3 regex false-positives remain = JS code expressions in FunnelFormControls/BlogPages/TestimonialCarousel, documented). MISSING CONTENT REFS: 0. DUPLICATE CANONICAL VALUES: 57 identical values retained in page-scoped namespaces (top: 'Nonprofit Board Builder' x10, 'Generation failed. Please try again.' x6, payment-status strings) — documented consolidation candidates, not merged to avoid cross-page coupling regressions
- Intentional exceptions: short labels (<4 words) & conditional status words, data-testids, routes/API/product keys, AI-generated output, runtime values, blog body data (BlogPages data), legal docs, developer-only strings
- Verified: CI prod build OK, backend boot OK, runtime render check on 6 key pages (no 'undefined' rendering), missing-ref validation 0. NOT functionally tested per instruction
