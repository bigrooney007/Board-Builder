# Nonprofit Board Builders — PRD

## Original Problem Statement
SaaS for nonprofit board building (React + FastAPI + MongoDB). Three funnels (Recruitment, Reactivation, Fundraising Activation) unified under the "Complete Board Fix System" umbrella funnel. Master intake prepopulates secondary intakes; chronological Admin tracking dashboard. Manual founder engagement is intentional (founder sends all emails/forms themselves).

## Status (June 2026)
- Complete Board Fix System fully coded, INCLUDING the June structural update below. FULL FUNNEL E2E TESTING STILL PENDING per user's standing "do not test yet" directive.
- Stripe checkout MOCKED (test mode locally). AI generation via Emergent LLM key. Resend for emails.
- Stripe webhook: FIXED locally — live signing secret set in backend/.env (June 2026), backend verified (8/8 webhook tests pass with new secret). AWAITING USER: redeploy to production, then "Resend" the 19 failed events in Stripe Dashboard. Retry-safe guards in place.

## June 2026 Structural Update (Complete Board Fix flow/pricing) — IMPLEMENTED, UNTESTED
1. Pricing: /offer/board-fix now shows Option 1 "Complete Board Fix System" regular $997, discounted $497 (50% OFF, 7 days) + Option 2 "Do It With Me" $5,497. All price text lives in siteContent.js offerSalesPages["board-fix"].options (configurable, not hardcoded in components).
2. DWM: new /api/payments/board-fix-dwm-checkout (549700¢, purchase_source board_fix_dwm_5497, env STRIPE_BOARD_FIX_DWM_5497_PRICE_ID). Same account/purchase-success architecture. After master intake, DWM customers are redirected to Calendly https://calendly.com/boardbuilder/recruitboard. board_fix_journeys.experience = "do_it_with_me"|"self_guided". Admin row includes experience.
3. Entitlements (member_routes): board_fix 497 & dwm_5497 both grant board_fix_system + recruitment_self_guided + reactivation_self_guided + activation_self_guided + recruitment_selection_onboarding (needed for recruitment modules 4-6 generators).
4. Canonical 10-step journey (board_fix_routes JOURNEY_STAGES + roadmap frontend): Orientation → Rebuild Your Present Board (reactivation all modules) → Identify (recr. mod 1-2) → Launch (3) → Select & Interview (4) → References & Background Checks (5) → Onboard (6) → Fundraising Planning (activ. mod 1-2) → Create Strategy (3) → Adopt (4) → Execute (5). needs_intake on identify/fundraising_planning stages.
5. References step: new generation types referee_confirmation_email, candidate_referee_request (includes secure /reference-form link when process exists); reference_request_email rewritten as Reference Check Email; ReferenceEmailsPanel added in module 5 UI (4 cards incl. reference_call_script). Conditional appointment preserved.
6. Onboard step: formal_appointment_email (Final Board Appointment Email) ADDED to GENERATION_TYPES (frontend card existed but backend type was missing = was broken). Reuses conditional_offer links block (onboarding materials/agreements/profile form links).
7. Gap analysis (powerhouse_board_blueprint): context now instructs founder counted as present board member + includes completed recommitment responses.
8. Strategy creation: approval/disapproval concept REMOVED — StrategyReviewPage idea Approve/Disapprove UI removed (plan displayed read-only + position/discussion/contribution retained); backend submit_review no longer validates/stores idea decisions; Module 4 approved/disapproved counters removed; revised-strategy prompt reworded. Strategy generated from planning responses + founder intake + framework (already the case).
9. Strategy email (activation_review_email) body: approval language removed; contains plan review link inline (copy/paste ready). Planning email already contains form link.
10. Execution (activation step 5): toolkit context includes all recommitment responses; per-member fundraising portfolio includes that member's recommitment response (matched by email).
11. Continuation links: Reactivation end → "Continue to Identify the Board Members You Need"; Recruitment end → "Continue to Fundraising Planning".

UNCHANGED: all direct-purchase funnels (recruitment/reactivation/activation), all existing generators, master intake form/fields, reference process automation, adoption flow, admin structure, orientation page.

## Earlier this fork
- Scoped public-page centering via body.public-centered (tested iteration_57, passed). Homepage copy replaced verbatim with user copy (hero/introduction/builder pillars); "Introduction" heading removed, first paragraph bolded. member-nav width fix.

## Backlog
- P0: Fix live Stripe webhook (confirm prod STRIPE_WEBHOOK_SECRET matches live endpoint secret; fix .gitignore .env blocking; make six_module_migration non-destructive) — awaiting user resume.
- P0: Full E2E testing of Complete Board Fix System incl. new 10-step journey, DWM flow, new emails — BLOCKED until user go-ahead.
- P2: Real YouTube IDs for board-fix sales video + orientation.

## Key files
- Backend: payment_routes.py (checkouts+webhook), member_routes.py (entitlements), board_fix_routes.py (journey/roadmap/admin), board_fix_master.py, ai_service.py (GENERATION_TYPES), workspace_routes.py (generate context), activation_planning_routes.py, content_templates.py
- Frontend: funnels/OfferVideoPage.jsx, content/siteContent.js, funnels/BoardFixIntakePage.jsx, member/BoardFixRoadmapPage.jsx, member/BoardFixContinuation.jsx, member/workspace/ApplicantModules.jsx, funnels/StrategyReviewPage.jsx, member/ActivationModule4.jsx

## Credentials
See /app/memory/test_credentials.md (admin rooney@nonprofitboardbuilder.com; member module-tester@example.com / ModuleTest123!)

## June 2026 fork — Sales Funnel Diagnostic + DFY Client Pathways (per user's verbatim payload; NO testing per standing policy)
- board_fix funnel form: basic info only (first/last name, org, email, phone, website optional); the two broad textarea questions REMOVED (funnelConfig.js, FunnelStepForm name composition, funnel_service required answers → set()).
- /offer/board-fix video page: price cards REPLACED by deterministic 4-question diagnostic (BoardFixDiagnostic.jsx) → recommendation result = immediate single-offer sales block (verbatim copy, ~~regular~~/discount/50% note/what-we'll-do/CTA). ZERO AI. Backend POST /api/funnel-leads/board-fix/diagnostic stores diagnostic_answers + recommended_pathway on lead; diagnosis rules: No board→recruitment; 2+ flags→complete; single flag→that pathway; none→activation. Verified all branches via curl (no data created).
- Payments: new resolvers/endpoints — POST /payments/dfy-checkout (pathway∈reactivation|recruitment|activation, $997, purchase_sources board_reactivation_dfy / board_recruitment_dfy / board_fundraising_activation_dfy, success→existing pathway intake ?session_id&dfy=1) and POST /payments/complete-transformation-checkout ($1,997, purchase_source complete_board_transformation_1997, offer_source board_fix_system tier complete_1997, success→/purchase/success → existing BUF journey). member_routes grants board_fix_system + all extras for complete_1997. Transactions retain lead context + diagnostic answers + recommended pathway.
- DFY delivery reuses EXISTING DWM operator architecture: 3 new purchase_sources added to DWM_OFFERS (same intake collections/entitlements/entry routes); admin dwm-clients rows extended (engagement_type/engagement_status/first_meeting/current_step + lead-context fallbacks); PATCH /api/admin/dwm-clients/{session_id} for manual status fields (stored on payment_transactions, no AI). ClientDeliverySection.jsx → "Done-For-You Clients" table with editable meeting/status/step columns.
- Intake pages show "You're In. Let's Get Started." banner when dfy=1; existing prefill + Calendly booking flow unchanged. No new BUF systems, no CRM, no automation. UNTESTED E2E per standing policy (compile + deterministic-diagnostic curl checks only).

## June 2026 fork — Fundraising Execution Resources (per user's verbatim payload; NO testing per standing policy)
- `activation_execution_toolkit` REPURPOSED: prompt+schema replaced verbatim (supporting tools: how-to-use, before-reach-out checklist, conversation/meeting prep, report-back, follow-up tracker, stewardship, max 3 strategy-specific tools). Recommitment responses REMOVED from context; responsibilities passed as role+responsibility+status WITHOUT names; strategy_specific_tools capped [:3]; new toolkit_display renderer. History preserved.
- NEW `activation_case_for_support` (org-level, 1 AI call): verbatim prompt+schema; db.activation_cases; Generate/Edit(→Draft)/Approve(version push + case_share_token)/PDF; public read-only /api/case-for-support/{token} (+/pdf) serves latest APPROVED snapshot only; frontend route /case-for-support/:token (CaseForSupportPage.jsx, B&W portfolio-style).
- NEW `activation_board_communication_system` (org-level, 1 AI call, all audiences×3 stages×email+call): verbatim prompt+schema; db.activation_comm_systems; gated on module5_ready + APPROVED Case + real share URL in context; audience_sequences capped [:4]; comm_display renderer; Board-facing (no public route).
- SYSTEM_MESSAGE: unconditional founder EMAIL SIGNATURE RULE replaced with conditional EMAIL SENDER / SIGNATURE RULE (founder emails keep signature; reusable Board Member scripts start "Hello," with no founder signature/invented identity).
- /activation/toolkit GET now returns case + communication_system summaries; ActivationModule5 shows 3 resource cards (Case → Comm System → Toolkit) via new ExecutionResourceCard.jsx; comm generate disabled until Case approved; share-link block with outdated-version note. Portfolio context toolkit-title extraction extended to new schema keys (only permitted compatibility change).
- UNTESTED per user's standing DO-NOT-TEST policy. Portfolio prompt/Planning/Strategy/Adoption/payments untouched.

## June 2026 fork — BUF Reactivation & Recruitment prompt overhaul (per user's verbatim instructions; NO testing/emails/deploys per explicit user policy)

1. Recommitment Form REPLACED with streamlined conditional form (BoardRecommitmentFormPage.jsx rewritten; RECOMMITMENT_OPTIONS now 3: YES/NO/NOT SURE; RecommitmentSubmission model + branch validation rewritten; confirmation checkbox removed; thank-you uses founder name+title; founder_name/title added to GET context).
2. Post-form workflow: reactivation_response_analysis prompt+schema replaced (pathway-aware); analysis_display rewritten; conversation script prompt+schema replaced (fundraising field removed, script_display renumbered), script now REQUIRES analysis (409) + bylaws context; Conversation Conclusion is authoritative (labels updated in appContent step4); outcome emails gated on saved conclusion; NEW reactivation_recommitment_confirmation generator + OUTCOME_EMAIL_TYPES entry "Continuing as an Active Board Member"; Step5 active group gets recommitment email button; Step3 gets VIEW UNDERSTANDING modal (analysis in step3 rows).
3. Generator 2 (reactivation_board_summary): prompt+schema replaced — post-conversation "what Board do we HAVE" picture; context = full roster w/ Conversation Conclusion authoritative, BOARD SUMMARY STATUS COMPLETE/INCOMPLETE, master intake + bylaws; board_summary_display rewritten (confirmed/advisory/stepped-down/unresolved sections); recruitment recommendations removed from this generator.
4. Generator 6 (reactivation_engagement_plan): prompt+schema replaced — Board-facing engagement plan from CONFIRMED continuing members only; plan_routes generation gated on confirmed recommitments; advisory-only section; unresolved warning in GET + generate response; plan_text renderer rewritten.
5. Generator 7 (powerhouse_board_blueprint): prompt+schema replaced — Powerhouse Matrix → confirmed present Board → gap → priority profiles; count rule preserved; workspace_routes context now uses final Reactivation outcomes (NOT completed forms), board summary material + bylaws; renderer rewritten per spec order.
6. Nine recruitment copy generators (board_opportunity, job post, linkedin, social, recruitment_emails, personal invitation email/message, referral email/message): notes now RECRUITMENT_POSITIONING (universal positioning standard + quality test) + CAMPAIGN_STANDARD + channel brief. Schemas/renderers/[APPLICATION LINK] system preserved. All receive blueprint via build_org_context (pre-existing).
STATUS: compiles, services run. ENTIRELY UNTESTED E2E per user's standing DO-NOT-TEST policy. NOT modified: recommitment outreach/reminder emails, recruitment_strategy, application_questions, linkedin_launch_instructions, interview/selection/onboarding resources, Fundraising Activation, Strategic Planning, payments, admin, public site.
7. application_questions: schema field descriptions strengthened (label applicant-facing / why INTERNAL) + new resource-specific developer prompt — exactly 5 questions (mission connection, expertise alignment vs blueprint priority profiles, potential contribution, active participation incl. general fundraising willingness where verified, realistic commitment). why never reaches applicants (workspace_routes:627 strips it before public save — verified, no display change needed). [:5] cap in generate_structured preserved.
8. Applicant invitation & initial selection comms: interview_invitation, interview_invitation_message, general_interview_invitation, general_interview_invitation_message, before_interview_rejection — all 5 got verbatim resource-specific developer prompts + strengthened schema descriptions (candidate-specific vs reusable [APPLICANT NAME] distinction preserved; real scheduling URL where supplied else direct-coordination language, no placeholders; invitation = mutual conversation not appointment; rejection = founder's decision, no invented reason/comparisons/false hope). workspace_routes: board_role added to context for the 3 candidate-specific types. No AI ranking/auto-decisions. No scheduling system exists — nothing built.
9. interview_guide: prompt+schema replaced (read-through facilitation guide: founder brief, org/board scripts, core + candidate-specific questions, evidence-based listen_for, board expectations script, fundraising only where verified expectation, founder-only 1-5/N-A evaluation matrix w/ blank rating/notes + 3 decision options AI never selects). Custom _interview_guide_display renderer added in ai_service.py. CREDIT CHECK: run_interview_guide is only triggered by explicit founder retry endpoint (NOT on application submission) — no change needed; generation remains manual-only.
10. Post-interview comms: after_interview_thank_you (strictly decision-neutral), after_interview_email retitled "Move Forward After Interview Email" (references/background-checks next-stage only, never appointment; never duplicates referee request), after_interview_rejection (respectful closure, founder decision, no invented reasons/false hope) — all with verbatim prompts + strengthened schemas. UI: move-forward MaterialCard added in ApplicantModules interviewed section with explicit use-only-after-decision guidance (no auto-generation/sending; no decision system built — founder's manual decision governs which card to use).
11. References & Background Checks: 5 generators (candidate_referee_request, reference_request_email, referee_confirmation_email, reference_call_script→read-through guide w/ new schema, reference_evaluation_form→"Reference Record & Evaluation Form" blank founder worksheet) got verbatim prompts; the FIVE standardized reference questions now consistent across email/call guide/eval form AND the hosted referee questionnaire (REFEREE_QUESTIONS labels aligned, qids preserved). Referee-specific context block added (GenerateRequest.referee_id optional; single referee auto-selected; multiple referees labeled with never-mix instruction). Custom renderers added for call guide + eval form. Background-check panel: clarifying founder-facing note added (tracker only; no AI checks; submitted ≠ passed). No AI pass/fail anywhere; existing statuses/storage untouched.
12a. (this fork, June 2026) Planning Form group re-audit per user payload: activation_planning_form prompt replaced verbatim; initial email final paragraph + public thank-you aligned verbatim ("review together"); suggestions restricted to final_outcome "Joined Board" (recruitment) and conversation_outcome "Continuing as an Active Board Member" (reactivation); recruitment import now requires Joined Board; shared_form_token submission fallback REMOVED (participant form_token is the only live submission path; historical tokens preserved in storage). Advisory/other members remain addable via explicit manual add. 14-question deterministic form, unique per-member tokens, deterministic initial/reminder emails, version pinning, reminder gating already existed and were preserved. UNTESTED per user's standing DO-NOT-TEST policy.
12. Conditional Appointment: conditional_offer got verbatim prompt + new schema; SEPARATED from the onboarding link/signature block — generating it no longer creates signature_requests, board_profile_links, manual/overview links or onboarding-session content (that block is now formal_appointment_email only, untouched otherwise). New read-only status context (reference + background; Not Required/blank never a condition; all-clear note tells model/founder to use Formal Appointment). UI: ConditionalPanel description rewritten + green "all requirements complete → use Formal Appointment" note. "final onboarding requirements" language removed from this resource.

## June 2026 fork — BUF Flow Correction + Admin Journey Access + Direct Payment Links + $697 Campaign Launch (per user's verbatim 35-part payload; NO testing per standing policy)
1. JOURNEY_STAGES (board_fix_routes.py) rewritten to the exact 11-step Complete Board Fix sequence: orientation → understand (reactivation modules [3,4,5] — Understand Their Response + Have the Hard Conversation + My Board/outcome comms) → identify (recr [2]) → launch (recr [3]) → select (recr [4]) → references (recr [5]) → onboard (recr [6]) → fundraising_planning (activ [2]) → create_strategy ([3]) → adopt_strategy ([4]) → execute_strategy ([5]). "Rebuild Your Present Board" stage REMOVED from active navigation; reactivation modules 1-2, recruitment module 1, activation module 1 no longer in Complete-flow nav (standalone courses untouched). Labels updated to user's step names.
2. BUF next-step override: new frontend `member/bufJourney.js` (BUF_SEQUENCE + bufStep). When member has board_fix_system entitlement, module NEXT/PREVIOUS buttons + breadcrumbs in ReactivationCoursePages/CoursePages(self-guided)/ActivationCoursePages follow the BUF map (reactivation m3→m4→m5→recruitment m2…m6→activation m2…m5→/board-fix-roadmap; prev of reactivation m3 →/board-fix-orientation). Standalone $997/DFY members (no board_fix_system) keep standalone nav. BoardFixContinuation destinations fixed to module-level BUF targets.
3. Step 1 (BoardFixOrientationPage) rewritten: video + 2 questions ("Save My Answers") then embeds ReactivationStep2 (generate/review/edit/approve Recommitment Form + email + form link + responses count); Next Step → /app/reactivation/self-guided/module/3. Backend save_orientation next_url → module/3.
4. Part 3 verified (code audit): /reactivation/understand and /reactivation/step3 both query ALL db.reactivation_board_members for user; frontend renders every status==COMPLETED record (YES/NO/NOT SURE all shown, no outcome prerequisite, unique by member_record_id); board summary uses same COMPLETED+response set.
5. Admin preview (Super-Admin only): POST /api/admin/board-fix/preview-access (board_fix_routes) grants BUF entitlements to the member account matching the admin email, creates db.board_fix_admin_previews record (internal_preview=true, session admin-preview-{user_id}); verify_session/intake_submit accept preview sessions (no Stripe/payment/purchase records); admin customers + funnel journey counts exclude internal_preview. Button "Test Complete Board Transformation" in ClientDeliverySection. Requires an existing member account with admin email (409 with instructions otherwise).
6. Direct payment URLs (App.js + new funnels/DirectOfferPage.jsx reusing OfferPurchaseBlock exported from BoardFixDiagnostic.jsx — same checkout endpoints): /board-reactivation, /board-recruitment, /board-fundraising-activation ($997 dfy-checkout), /complete-board-transformation ($1,997). CODED — NOT LIVE UNTIL DEPLOYMENT.
7. $697 Recruitment Campaign Launch: /offer/recruitment transformed — old $297/$1,497 videoButtons removed; under video now "Your Outcome" (48-72h applicants, exact copy) + "100% Guarantee" (exact copy) + CTA "LAUNCH MY BOARD RECRUITMENT CAMPAIGN — $697" (siteContent.recruitment.campaignLaunch; CampaignLaunchOffer in OfferVideoPage). New POST /api/payments/campaign-launch-checkout (lookup recruitment_campaign_launch_697, env STRIPE_RECRUITMENT_CAMPAIGN_LAUNCH_697_PRICE_ID, $697 one-time, purchase_source recruitment_campaign_launch_697, success → /board-recruitment-intake?session_id&dfy=1 → Calendly booking). DWM_OFFERS + dwm-clients row engagement_type recruitment_campaign_launch ("Recruitment Campaign Launch") entry_route recruitment module 2. Diagnostic $997 Recruitment result unchanged. No auto-refund system.
8. Intake QUALIFYING_SOURCES fix (pre-existing gap): board_recruitment_dfy + recruitment_campaign_launch_697 (recruitment), board_reactivation_dfy (reactivation), board_fundraising_activation_dfy (activation) now accepted by the intake session verification; OFFER_LABELS added.
STATUS: compiles, services healthy, one read-only render check of /board-recruitment passed. ENTIRELY UNTESTED E2E per user's standing DO-NOT-TEST policy.
CORRECTION (same session): Complete-flow reactivation ends at module 3. BUF_SEQUENCE = reactivation[3] → recruitment[2..6] → activation[2..5] → /board-fix-roadmap. JOURNEY_STAGES understand modules=[3]. BoardFixContinuation card now on reactivation module 3. Reactivation modules 4-5 remain ONLY in the standalone Reactivation product.
