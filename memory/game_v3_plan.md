# GAME V3 SIMPLIFICATION + AI FINE-TUNING — COMPLETE (June 2026)

STATUS: FULLY IMPLEMENTED per user's clarified spec (incl. the 3 adjustments). NOT TESTED — user mandates NO testing/screenshots/AI test calls/emails; user tests manually.

## What shipped
1. FUNNEL: Homepage goal box now also collects Name/Email/Org (sessionStorage bfgName/bfgEmail/bfgOrg) → GameAuthPage prefills name/email → GameProfilePage prefills org name + full name. Profile saved screen = "Your Board Fundraising Game Is Ready" + CTA "Play My Board Fundraising Game" (POST /game/self-play → /play/{token}).
2. PRIMARY PRE-PAYMENT: plays 4-round V3 game via self-play token → locked "Your Fundraising Strategy Is Ready" screen → /game/upgrade (rewritten: header → goal → completed ✓ areas → more-people statement → payment → 6 process steps → 5 outcomes → repeat CTA; video/features/steps/intro REMOVED). NO fine-tuning pre-payment (backend 402-gates on org entitlement).
3. AI FINE-TUNING (paid only): backend/game_finetune_routes.py — POST /api/game/play/{token}/fine-tune/{1-4} (ONE Claude call per strategic area; input-hash dedup = no repeat AI calls; empty inputs = no AI call) + /decisions (use_ai | keep_original | edited per idea). Approved entries stored on game_section_responses.approved_entries (sections 2/3 texts stored as "Audience — idea"). Frontend game/FineTuneReview.jsx (shared component: Your Idea / Fine-Tuned Version / Use This / Keep My Original Idea / Edit My Idea).
4. BOARD MEMBER FLOW (GamePlayPage): 4 rounds → fine-tune review of 4 areas → participation (build/raise/time) → PERSONAL MINI STRATEGY screen (7 fixed sections, deterministic, from approved entries + participation) → "You're Ready For Game Night" (+ View My Personal Fundraising Strategy button).
5. PRIMARY POST-PAYMENT (GameSituationPage REWRITTEN, route /game/setup): "Review The Fundraising Strategy You Created" intro → fine-tune 4 areas (self-play token) → 5-question Current Reality (GAME_V3.current_reality → situation.sections.current_reality) → participation (situation.sections.participation + mirrored to self-play section 5) → situation complete → auto POST /game/strategy/generate mode=working → poll → "Your Complete Working Fundraising Strategy Is Ready" → /game/strategy/view/{id}. play_first guard when game not yet played.
6. GROUP GAME: TOTAL_ROUNDS=6 review screens = Areas 1A Individuals / 1B Businesses / 1C Grantors (approved entries filtered by audience_type; fallback extras buckets; legacy fallback → individuals) + Areas 2/3/4. Unique section_keys: who_should_fund_individuals/_businesses/_grantors, where_to_find, attract_attention, fundraising_process (rounds/ideas/results/overrides all keyed uniquely — priority review works untouched). HARD STOP after Area 4 → "Your Board Has Completed The Fundraising Game" + host CTA "Generate Our Complete Fundraising Strategy" → EXISTING flow (/game/strategy/priorities → board-prioritized strategy → meeting review → final → adopt → portfolios → toolkits). No extra voting rounds ever.
7. STRATEGY ENGINE (strategy_routes): board ideas assembled per 4 areas from FINAL APPROVED entries (area_ideas, typed prefixes Individual:/Business:/Grantor:); context += participation_choices (section-5 extras of all players); system message now instructs engine to DERIVE team/tech/materials/resources/timeline/budget from the 4 human decisions + current reality + participation (source: recommendation).
8. play_context returns `paid` flag. game_content_v3.py holds ALL new copy (fine_tuning, mini_strategy, primary_review, group_complete) — admin-editable via new Admin → Fundraising Game → "Individual Game Content (V3)" JSON editor panel (GET/PUT /admin/game/v3-content).
9. Mongo migration ran: $unset stale game_site_content overrides for profile_flow/upgrade_page (none existed; defaults live).

## Key files
- backend: game_finetune_routes.py (NEW), group_game_routes.py (6 ROUND_DEFS + AREA_DEFS + area_ideas), strategy_routes.py, game_night_routes.py (paid flag), game_routes.py (profile_flow copy), game_content_v3.py, server.py (router registered)
- frontend: game/FineTuneReview.jsx (NEW), GamePlayPage.jsx (finetune/ministrategy phases), GameSituationPage.jsx (REWRITTEN post-payment flow), GameUpgradePage.jsx (rewritten), GameHomePage/GameAuthPage/GameProfilePage (lead capture + prefill + play CTA), GroupGamePage.jsx (completion copy), admin/GameSection.jsx (V3 editor), game.css

## AI cost guarantees
- Fine-tuning: 1 call per strategic area per member (4 max/member), button/flow-triggered, input-hash deduped, empty-skip. Primary pre-payment: zero AI.
- Working strategy: 1 deliberate call at end of primary setup (existing engine + duplicate lock). Everything else deterministic.

## VOICE-GUIDED INDIVIDUAL GAME (June 2026) — IMPLEMENTED, NOT TESTED (user mandate)
- backend/voice_content.py: owner's EXACT script preloaded verbatim (clips c01-c60 + 5 static fallbacks f_p1..f_p5 + 5 personalized templates p1_welcome/p2_after_q1/p3_round_2/p4_after_q8/p5_completion — [FIRST NAME] only dynamic variable). NEVER edit spoken text in code without owner script.
- backend/voice_routes.py (registered in server.py): GET /game/voice/settings + /manifest/{token} + audio serving (Mongo-stored MP3 bytes, cache headers); POST /game/voice/personal/{token} (server-side ElevenLabs micro-clip: sanitized first name, sha cache, per-member cap default 5 via game_board_members.personal_clips_generated, generic fallback on ANY failure — never blocks); Admin GET/PUT voice settings (marketing_settings key game_voice_settings), GET assets, PUT asset text (DB override of script), POST generate (deliberate ONCE per clip, 409 if ready, versioned). ELEVENLABS_API_KEY / ELEVENLABS_VOICE_ID in backend/.env (EMPTY — Rooney must supply; static clips show Not Generated until admin generates).
- frontend game/VoiceGuide.jsx: ModeSelect (Play With Voice emphasized / Read & Type), PermissionScreen (mic requested ONLY on Continue With Voice, tracks stopped immediately), VoicePanel = deterministic step runner ({clip}|{personal}|{capture}): narrator speaking banner (Pause/Replay/Sound Off), turn-based auto-listen (webkitSpeechRecognition continuous, 3s silence auto-stop, I'm Done / Type Instead / Skip), "Turning Your Answer Into Text…", "Here's What We Heard" editable review (Continue / Say It Again / + Add Another for list targets). Missing audio ⇒ instant advance (game never blocked). Mic and narrator never simultaneous. No raw audio stored anywhere.
- GamePlayPage: mode choice persisted localStorage bfgVoiceMode:{token}; step sequences per round exactly per script (R1: c02,c03,first→p2,c04-c06 people,c07-c09 businesses,c10-c12 grantors,c13; R2: p3,c14-c21 w/ per-audience place captures,c22; R3: c23-c29 per-audience offer captures,c30; R4: c31-c41 process capture); endings: primary ready=[p4,p5,c50a], board finetune entry=[p4,c42], participation=[c43,c44,c46,c48,c49+capture additional(skippable)], ministrategy=[p5,c50b]. NEW final open question "Is There Anything Else You Want To Share?" (exact copy) in participation for BOTH modes → extras.additional_idea; submit button now "Complete My Game". Captures write into EXISTING form state/fields (first thoughts, People/Businesses/Grantors, places, ideas, process) — forms remain source of truth; Read & Type unchanged.
- GameSituationPage (primary post-payment): c51 intro, c52 per fine-tune area, reality sequence c53,c54,c55-c59 (captures into the 5 existing reality fields),c60; participation narration c43/c44/c46/c48. Uses same stored mode.
- Admin → Fundraising Game → "Voice Guided Game": all spec settings + narration library (status Ready/Not Generated/Missing/Needs Regeneration, edit text, Generate Audio once per clip).
- Group Review: completely untouched/silent. Zero ElevenLabs/AI calls made during implementation. Participation "Other" voice: on-screen field + existing tap-to-speak fallback (clips c45/c47 stored, not auto-sequenced).
NEXT: Rooney adds ELEVENLABS_API_KEY + voice ID, generates static clips in admin, tests manually.

## VOICE TEST/LIVE ENVIRONMENTS (June 2026) — IMPLEMENTED, NOT TESTED (user mandate; ZERO ElevenLabs calls made)
- Env vars: ELEVENLABS_API_KEY, ELEVENLABS_TEST_VOICE_ID, ELEVENLABS_LIVE_VOICE_ID (test falls back to legacy ELEVENLABS_VOICE_ID). Preview .env has placeholders; real values are in deployment secrets per Rooney. Key/IDs never exposed (admin sees masked last-4 only).
- voice_routes.py REWRITTEN: audio now stored per environment in db.game_voice_audio {narration_id, environment, status, audio, voice_id, version, script_version, generated_at}; script text + script_version in db.game_voice_assets (text change bumps version → live/test status becomes NEEDS REGENERATION). Active environment = admin setting settings.voice_environment ("test" default) in marketing_settings game_voice_settings. Manifest/audio serving/personal clips all use active environment; personal cache key = sha(text|environment|voice_id) with env stored on cache docs — test clips never reused in live.
- Admin endpoints: GET/PUT voice settings (incl voice_environment, masked voice refs), GET assets (per-clip test+live status, script_version, missing_live list, bulk job status), PUT text, POST generate {environment, force} (live READY requires force=confirmed Regenerate), POST generate-missing-live (in-process lock, background asyncio task, generates ONLY missing live clips, skips READY, progress in marketing_settings game_voice_bulk).
- Admin UI: admin/VoiceGuidedAdmin.jsx (extracted from GameSection.jsx): TEST VOICE badge when test env active, environment selector, per-clip Generate Test Clip / Generate Live Clip / Regenerate Clip (with exact confirmation copy), Generate All Missing Live Narration (confirmation: "Generate Live Narration? You are about to generate [N]... / Cancel / Generate [N] Missing Clips"), bulk progress polling.
- No automatic generation anywhere (startup/deploy/game/missing-asset all safe); game works with zero generated audio.

## TEST CLIP GENERATION (Sept 15, 2026, on PRODUCTION nonprofitboardbuilder.com)
- Generated via prod admin API (admin cookie login) exactly 3 TEST clips with TEST voice (…O9Qt): c01 (1.0MB), c05 (1.1MB), c31 (572KB) — all READY, stored in prod Mongo game_voice_audio (environment=test), served at /api/game/voice/audio/{id} (active env=test).
- Prod env check: ELEVENLABS_API_KEY configured, TEST voice configured, ELEVENLABS_LIVE_VOICE_ID NOT configured in production (name to add before live generation).
- Added <audio controls> preview player per clip row in admin/VoiceGuidedAdmin.jsx (in preview code; appears on prod after next publish). No other clips, no personalized clips, no live narration generated.

## PERSONALIZED TEST CLIP (Sept 2026)
- Added admin-only POST /api/admin/game/voice/personal-preview {point_id, first_name} (standard cache architecture, active-env voice) + admin UI: preview generation on template rows, "Personalized Clips (cached)" section with audio players and "Play With Clip 01" chained playback. Redeployed to production.
- Generated on production (TEST voice): "Welcome to the Board Fundraising Game, Rooney." — cached at game_voice_personal_cache key 325416e4… (~55KB mp3), playable in Admin → Voice Guided Game and reused automatically by gameplay for any player whose sanitized name yields identical text.
